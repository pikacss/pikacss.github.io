---
phase: 08-verification-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api-verifier/src/extractor.ts
autonomous: true
must_haves:
  truths:
    - "EnginePlugin type is correctly resolved (not any)"
    - "Complex types extract with meaningful structure"
  artifacts:
    - path: "packages/api-verifier/src/extractor.ts"
      provides: "Improved type extraction logic"
---

<objective>
Improve API extractor to resolve complex types like EnginePlugin instead of returning 'any'.

Purpose: Fix low-quality extraction for recursive or complex types, enabling verifying plugin API compliance.
Output: Updated extractor.ts with better type resolution.
</objective>

<execution_context>
@/Users/deviltea/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/deviltea/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-verification-fixes/08-RESEARCH.md
@packages/api-verifier/src/extractor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve type resolution</name>
  <files>packages/api-verifier/src/extractor.ts</files>
  <action>
    Investigate and modify packages/api-verifier/src/extractor.ts to handle `EnginePlugin` and other complex types.
    - Check for recursion limits or circular dependency handling.
    - Improve `extractAPIFromSymbol` and related logic to descend into `EnginePlugin` interface instead of bailing to `any`.
    - Ensure type arguments/generics are preserved.
  </action>
  <verify>
    Run `pnpm build` in api-verifier.
    Run a test extraction against `@pikacss/core` and check JSON output for `EnginePlugin`.
  </verify>
  <done>`EnginePlugin` is extracted as an interface/type signature, not "any".</done>
</task>

</tasks>

<verification>
Run `pnpm test` in packages/api-verifier.
Verify `EnginePlugin` extracts with properties (e.g., `name`, `version`, `configureEngine`).
</verification>

<success_criteria>
- [ ] EnginePlugin type extracts as structured object/interface
- [ ] No `any` fallbacks for primary API types
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-fixes/08-02-SUMMARY.md`
</output>
