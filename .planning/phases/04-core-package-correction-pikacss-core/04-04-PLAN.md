---
phase: 04-core-package-correction-pikacss-core
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-03]
files_modified:
  - docs/guide/basics.md
autonomous: true

must_haves:
  truths:
    - "All pika() usage examples in basics guide are executable"
    - "Examples follow build-time constraint (statically analyzable)"
    - "Core API usage matches actual signatures from api-reference.md"
    - "No hallucinated features or APIs in examples"
  artifacts:
    - path: "docs/guide/basics.md"
      provides: "Accurate core usage guide with validated examples"
      min_lines: 272
  key_links:
    - from: "docs/guide/basics.md"
      to: "packages/core/tests/examples/*.ts"
      via: "VitePress transclusion or bundler validation"
      pattern: "<<<|pika\\("
    - from: "docs/guide/basics.md"
      to: "docs/advanced/api-reference.md"
      via: "consistent API usage"
      pattern: "pika\\(|Engine|EngineConfig"
---

<objective>
Correct docs/guide/basics.md to ensure all @pikacss/core examples are accurate and executable.

Purpose: Basics guide is first user-facing documentation for @pikacss/core. Examples must work as written and follow all PikaCSS constraints. Users copy-paste these examples into real projects.

Output: docs/guide/basics.md with validated examples passing multi-bundler tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-package-correction-pikacss-core/04-CONTEXT.md
@.planning/phases/04-core-package-correction-pikacss-core/04-RESEARCH.md

# Requires accurate architecture and API documentation
@.planning/phases/04-core-package-correction-pikacss-core/04-01-SUMMARY.md
@.planning/phases/04-core-package-correction-pikacss-core/04-03-SUMMARY.md

# Source of truth for API usage
@docs/advanced/api-reference.md
@packages/core/src/index.ts

# Multi-bundler test infrastructure
@.eslint/tests/integration/bundlers.test.ts
@.eslint/tests/fixtures/vite/src/valid.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract and validate all examples in basics guide</name>
  <files>docs/guide/basics.md</files>
  <action>
1. **Identify all code examples:**
   - Extract all TypeScript/JavaScript code blocks
   - Count pika() usage: `grep -c "pika(" docs/guide/basics.md` (should be ~13)
   - Categorize examples:
     - Inline examples (small snippets)
     - Complete examples (imports + usage)
     - Configuration examples

2. **Validate each example against actual APIs:**
   - For each pika() call:
     - Verify style properties exist in PropertyStyleDefinitions
     - Check build-time constraint (no runtime variables)
     - Confirm return type usage is correct
   - For Engine usage:
     - Verify method calls match actual Engine API
     - Check configuration options exist in EngineConfig
   - For plugin examples:
     - Confirm plugin API usage matches actual signatures

3. **Check consistency with API reference:**
   - Compare example signatures with docs/advanced/api-reference.md
   - Ensure no contradictions between guide and reference
   - Verify terminology is consistent

4. **Run PikaCSS ESLint rules on examples:**
   ```bash
   pnpm lint docs/guide/basics.md
   ```
   - Check for build-time constraint violations
   - Verify no package boundary violations
  </action>
  <verify>
1. `pnpm lint docs/guide/basics.md` exits 0
2. Count examples: `grep -c "pika(" docs/guide/basics.md` matches expected count
3. Check no runtime variables: `grep "pika({.*props\." docs/guide/basics.md` returns nothing
4. All imports reference actual exports: Compare with `cat packages/core/src/index.ts | grep "^export"`
  </verify>
  <done>
- All pika() examples validated against actual API
- No build-time constraint violations
- Examples consistent with api-reference.md
- ESLint rules pass for all code blocks
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix invalid examples and ensure multi-bundler compatibility</name>
  <files>docs/guide/basics.md</files>
  <action>
1. **Fix invalid examples found:**
   - Replace hallucinated APIs with actual exports
   - Fix signature mismatches
   - Update runtime-dynamic examples to use CSS variables pattern:
     ```typescript
     // Bad (runtime variable)
     const styles = pika({ color: props.color })
     
     // Good (build-time + CSS variable)
     const styles = pika({ color: 'var(--my-color)' })
     // Set CSS variable at runtime: element.style.setProperty('--my-color', props.color)
     ```
   - Remove features that don't exist

2. **Convert complex examples to VitePress transclusion:**
   - For examples that demonstrate complete workflows:
     - Create test file: `packages/core/tests/examples/basics-example-N.ts`
     - Validate in multi-bundler tests (Vite, Nuxt, Webpack)
     - Reference in docs: `<<< @/../packages/core/tests/examples/basics-example-N.ts`
   
   **When to transclude:**
   - Example is >15 lines
   - Example includes imports + usage + exports
   - Example needs bundler validation
   
   **When to inline:**
   - Simple snippets demonstrating single concept
   - Examples without imports (assumes context)

3. **Ensure examples are self-contained (for user guides):**
   - Each example should be copy-paste ready
   - Include necessary imports
   - Show complete setup (not just fragments)
   - Add comments explaining key concepts

4. **Validate against multi-bundler infrastructure:**
   - Check that example patterns match .eslint/tests/fixtures/vite/src/valid.ts
   - Ensure no patterns from invalid.ts (runtime dynamic, etc.)
  </action>
  <verify>
1. All transcluded examples exist: Check referenced test files exist
2. Run multi-bundler tests: `cd .eslint && pnpm test:integration` includes core examples
3. Verify self-contained: Each example has imports or clear context
4. ESLint passes: `pnpm lint docs/guide/basics.md` exits 0
  </verify>
  <done>
- All invalid examples fixed or removed
- Complex examples converted to VitePress transclusion
- Examples are self-contained and copy-paste ready
- Multi-bundler compatibility verified
  </done>
</task>

<task type="auto">
  <name>Task 3: Run complete verification pipeline</name>
  <files>docs/guide/basics.md</files>
  <action>
1. **Sequential validation:**
   ```bash
   # 1. ESLint (structural + PikaCSS constraints)
   pnpm lint docs/guide/basics.md
   
   # 2. Link checker
   bash scripts/check-links.sh
   
   # 3. API verifier
   bash scripts/verify-api-docs.sh
   
   # 4. Multi-bundler tests (if examples transcluded)
   cd .eslint && pnpm test:integration
   
   # 5. Placeholder detection
   bash scripts/check-placeholders.sh
   ```

2. **Fix all failures:**
   - ESLint: Fix code syntax, PikaCSS constraint violations
   - Links: Update broken references
   - API verifier: Correct API usage to match reference docs
   - Bundler tests: Fix examples that don't compile
   - Placeholders: Complete or remove

3. **Cross-reference validation:**
   - Search for all core API mentions: `grep -n "pika\|Engine\|EngineConfig" docs/guide/basics.md`
   - For each mention, verify against api-reference.md
   - Ensure no contradictions

**Zero tolerance:** All validations must pass before completion.
  </action>
  <verify>
1. `pnpm lint docs/guide/basics.md` exits 0
2. `bash scripts/check-links.sh` reports no broken links
3. `bash scripts/verify-api-docs.sh` reports:
   - 0 signature mismatches
   - 0 contradictions with api-reference.md
4. `cd .eslint && pnpm test:integration` passes (if examples transcluded)
5. `bash scripts/check-placeholders.sh` finds no placeholders
6. Manual spot-check: Pick 3 random examples, verify they compile in isolation
  </verify>
  <done>
- Complete verification pipeline passes with zero failures
- All examples executable and correct
- No contradictions with api-reference.md
- Basics guide provides accurate introduction to @pikacss/core
  </done>
</task>

</tasks>

<verification>
**File completion criteria:**
- All pika() examples in basics.md follow build-time constraint
- 0 API signature mismatches with api-reference.md
- 0 contradictions across documentation
- Multi-bundler tests pass (if examples transcluded)
- Complete verification pipeline passes

**Success signals:**
- `bash scripts/verify-api-docs.sh` shows zero issues for basics.md
- Examples match patterns in .eslint/tests/fixtures/vite/src/valid.ts
- No patterns from invalid.ts present
</verification>

<success_criteria>
1. All @pikacss/core examples in basics.md are executable
2. All examples follow build-time constraint (no runtime variables)
3. API usage consistent with api-reference.md (zero contradictions)
4. Complex examples validated via multi-bundler tests or VitePress transclusion
5. Examples are self-contained and copy-paste ready
6. Complete verification pipeline passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-package-correction-pikacss-core/04-04-SUMMARY.md`
</output>
