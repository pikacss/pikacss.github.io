---
phase: 04-core-package-correction-pikacss-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/README.md
autonomous: true

must_haves:
  truths:
    - "Core package README describes all public exports accurately"
    - "Installation instructions work as documented"
    - "Usage examples execute successfully"
    - "Feature list matches actual implementation"
  artifacts:
    - path: "packages/core/README.md"
      provides: "Accurate package overview and quick start"
      min_lines: 201
  key_links:
    - from: "packages/core/README.md"
      to: "packages/core/src/index.ts"
      via: "exports verification"
      pattern: "export"
    - from: "packages/core/README.md"
      to: "packages/core/package.json"
      via: "version and installation"
      pattern: "version"
---

<objective>
Correct packages/core/README.md to accurately reflect actual @pikacss/core implementation.

Purpose: Package README is the first documentation developers see when discovering @pikacss/core on npm or GitHub. Must provide accurate quick start and feature overview.

Output: packages/core/README.md with accurate descriptions passing all verification checks.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-package-correction-pikacss-core/04-CONTEXT.md
@.planning/phases/04-core-package-correction-pikacss-core/04-RESEARCH.md

# Core package source
@packages/core/src/index.ts
@packages/core/package.json
@packages/core/tsconfig.json

# Verification infrastructure
@scripts/verify-api-docs.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and correct core README content</name>
  <files>packages/core/README.md</files>
  <action>
1. **Extract actual package information:**
   - Read `packages/core/src/index.ts` to get complete export list
   - Read `packages/core/package.json` for version, dependencies, keywords
   - Use API verifier: `node packages/api-verifier/dist/cli.mjs --package core`

2. **Verify README sections against reality:**
   - **Installation section:** Verify package name is correct (`@pikacss/core`)
   - **Features list:** Each claimed feature must exist in source code
   - **Quick start examples:** All code examples must be valid and executable
   - **API overview:** Mentioned APIs must exist in actual exports
   - **Dependencies:** Should only list csstype (no other runtime dependencies)

3. **Fix inaccuracies found:**
   - Remove hallucinated features not present in code
   - Update API signatures to match actual implementation
   - Fix example code that references non-existent APIs
   - Correct version numbers or package names if wrong
   - Update dependency information to match package.json

4. **Critical-first approach:**
   - Fix wrong API signatures first (blockers)
   - Then fix feature claims (important)
   - Finally improve clarity (nice-to-have)

**What to avoid:**
- Don't add features that aren't implemented
- Don't use API signatures from memory (verify against source)
- Don't include internal implementation details
- Don't reference APIs from other packages unless they're peer dependencies
  </action>
  <verify>
1. Run API verifier: `bash scripts/verify-api-docs.sh` - check core README
2. Verify exports: Compare README API list with `cat packages/core/src/index.ts | grep "^export"`
3. Check dependencies: `cat packages/core/package.json | jq '.dependencies'` should only show csstype
4. ESLint: `pnpm lint packages/core/README.md` - no errors
  </verify>
  <done>
- All features listed in README exist in implementation
- All code examples reference actual exports
- Installation instructions are correct
- Dependencies section matches package.json
- No hallucinated APIs or features
  </done>
</task>

<task type="auto">
  <name>Task 2: Test examples and run full verification</name>
  <files>packages/core/README.md</files>
  <action>
1. **Extract and validate code examples:**
   - Find all TypeScript/JavaScript code blocks in README
   - For each example that imports from @pikacss/core:
     - Verify imported items exist in actual exports
     - Check API usage matches actual signatures
     - Validate no runtime-dynamic patterns (build-time constraint)

2. **Run verification pipeline (sequential):**
   - ESLint: `pnpm lint packages/core/README.md`
   - Link checker: `bash scripts/check-links.sh`
   - API verifier: `bash scripts/verify-api-docs.sh`
   - Placeholder detection: `bash scripts/check-placeholders.sh`

3. **Fix validation failures:**
   - ESLint errors: Fix code block syntax or indentation
   - Broken links: Update to valid paths
   - API mismatches: Correct to actual signatures
   - Placeholders: Complete or remove TODO markers

**Zero tolerance:** All validations must pass.
  </action>
  <verify>
1. `pnpm lint packages/core/README.md` exits 0
2. `bash scripts/check-links.sh` reports no broken links in core README
3. `bash scripts/verify-api-docs.sh` reports zero mismatches for core README
4. `bash scripts/check-placeholders.sh` finds no placeholders
5. All code examples use only actual exports from packages/core/src/index.ts
  </verify>
  <done>
- Complete verification pipeline passes with zero failures
- All code examples validated against actual exports
- No broken links, placeholders, or structural errors
- README provides accurate overview of @pikacss/core capabilities
  </done>
</task>

</tasks>

<verification>
**File completion criteria:**
- API verifier reports 0 signature mismatches in packages/core/README.md
- All code examples reference only actual exports
- Installation and usage instructions are accurate
- Complete verification pipeline passes

**Success signals:**
- `bash scripts/verify-api-docs.sh` shows zero issues for core README
- Every claimed feature can be traced to source code
- Code examples compile and follow PikaCSS constraints
</verification>

<success_criteria>
1. packages/core/README.md describes only features that actually exist
2. All API examples use correct signatures from actual implementation
3. Installation instructions work as written
4. Dependencies list matches package.json (csstype only)
5. Complete verification pipeline passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-package-correction-pikacss-core/04-02-SUMMARY.md`
</output>
