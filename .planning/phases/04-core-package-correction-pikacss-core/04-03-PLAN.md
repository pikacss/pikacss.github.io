---
phase: 04-core-package-correction-pikacss-core
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - docs/advanced/api-reference.md
autonomous: true

must_haves:
  truths:
    - "All @pikacss/core API signatures in docs match actual exports"
    - "Engine class methods documented with complete signatures"
    - "Configuration options match EngineConfig interface"
    - "Plugin API documentation reflects actual hook signatures"
    - "No hallucinated APIs or features"
  artifacts:
    - path: "docs/advanced/api-reference.md"
      provides: "Complete @pikacss/core API documentation"
      min_lines: 466
      sections: ["pika() Function", "Engine Class", "Configuration", "Plugin System"]
  key_links:
    - from: "docs/advanced/api-reference.md"
      to: "packages/core/dist/index.d.mts"
      via: "TypeScript Compiler API extraction"
      pattern: "extractPackageAPIs"
    - from: "docs/advanced/api-reference.md"
      to: "AGENTS.md"
      via: "architecture consistency"
      pattern: "Engine|EngineConfig|EnginePlugin"
---

<objective>
Correct docs/advanced/api-reference.md @pikacss/core section to match actual implementation using API verifier.

Purpose: API reference is the authoritative documentation developers consult for exact signatures and usage. Must be 100% accurate with actual TypeScript exports.

Output: docs/advanced/api-reference.md with verified core API documentation passing all checks.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-package-correction-pikacss-core/04-CONTEXT.md
@.planning/phases/04-core-package-correction-pikacss-core/04-RESEARCH.md

# Requires accurate architecture from 04-01
@.planning/phases/04-core-package-correction-pikacss-core/04-01-SUMMARY.md

# Core package source
@packages/core/src/index.ts
@packages/core/src/types.ts
@packages/core/src/internal/engine.ts
@packages/core/src/internal/plugin.ts
@packages/core/dist/index.d.mts

# Verification infrastructure
@packages/api-verifier/src/index.ts
@scripts/verify-api-docs.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract actual core APIs and identify documentation gaps</name>
  <files>docs/advanced/api-reference.md</files>
  <action>
1. **Run API verifier to get baseline:**
   ```bash
   bash scripts/verify-api-docs.sh
   ```
   - Review API-VERIFICATION-BASELINE.md for @pikacss/core section
   - Identify signature mismatches (blockers)
   - Identify missing documentation coverage (warnings)
   - Identify contradictions across docs (blockers)

2. **Extract complete core API list:**
   ```bash
   node packages/api-verifier/dist/cli.mjs --package core --format json > core-apis.json
   ```
   - Review all exported functions, classes, interfaces, types
   - Note actual signatures with full TypeScript types
   - Compare with documented APIs in api-reference.md

3. **Categorize issues by severity:**
   - **Critical (fix first):** Wrong signatures, hallucinated APIs, contradictions
   - **Important (fix second):** Missing coverage for public APIs
   - **Minor (fix last):** Incomplete descriptions, missing examples

4. **Plan correction strategy:**
   - Group APIs by category (pika function, Engine class, config, plugins)
   - For each category, list mismatches to fix
   - Identify which examples need VitePress transclusion
  </action>
  <verify>
1. API verifier baseline report exists: `ls API-VERIFICATION-BASELINE.md`
2. Core APIs JSON extracted: `cat core-apis.json | jq '.packages[] | select(.name == "@pikacss/core") | .apis | length'`
3. Mismatch count documented: Record initial mismatch count for tracking
  </verify>
  <done>
- Complete list of @pikacss/core API mismatches identified
- Issues categorized by severity (critical/important/minor)
- Correction strategy planned for each API category
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix critical API signature mismatches</name>
  <files>docs/advanced/api-reference.md</files>
  <action>
1. **Fix pika() function documentation:**
   - Verify actual signature from packages/core/src/index.ts
   - Update function signature in docs to match
   - Fix parameter types (should accept PropertyStyleDefinitions)
   - Fix return type (returns string of class names)
   - Update pika.str(), pika.arr(), pika.inl() variants if documented

2. **Fix Engine class documentation:**
   - Extract Engine class methods from packages/core/src/internal/engine.ts
   - For each public method:
     - Correct signature (parameters and return types)
     - Update parameter descriptions
     - Fix return value documentation
   - Document all public methods (no "see TypeScript" shortcuts)

3. **Fix Configuration API (EngineConfig):**
   - Extract EngineConfig interface from packages/core/src/types.ts
   - Group options by purpose (styling, plugins, output, etc.)
   - For each option:
     - Correct type signature
     - Update description
     - Add examples if missing
   - Remove documented options that don't exist

4. **Fix Plugin System API:**
   - Extract plugin types from packages/core/src/internal/plugin.ts
   - Correct EnginePlugin interface signature
   - Fix plugin hook signatures (configureEngine, transformStyleDefinitions, etc.)
   - Update defineEnginePlugin() function signature

**For each fix:**
- Copy exact signature from TypeScript source or API verifier output
- Don't rely on memory or assumptions
- Keep descriptions comprehensive (type + description + params + return + example)
  </action>
  <verify>
1. Re-run API verifier: `bash scripts/verify-api-docs.sh`
2. Check mismatch count decreased: Compare with initial count
3. Verify critical sections:
   - `grep -A10 "## The \`pika()\` Function" docs/advanced/api-reference.md | grep "function pika"`
   - `grep -A5 "class Engine" docs/advanced/api-reference.md`
   - `grep -A5 "interface EngineConfig" docs/advanced/api-reference.md`
  </verify>
  <done>
- All signature mismatches for @pikacss/core fixed
- pika() function documented with correct signature
- Engine class methods documented completely
- EngineConfig interface matches actual implementation
- Plugin system API signatures corrected
  </done>
</task>

<task type="auto">
  <name>Task 3: Address missing coverage and run full verification</name>
  <files>docs/advanced/api-reference.md</files>
  <action>
1. **Add documentation for undocumented public APIs:**
   - Review API verifier's "missing documentation" list for core
   - For each undocumented API:
     - Add section with complete documentation structure:
       - Type signature
       - Description
       - Parameter explanations
       - Return value documentation
       - Usage example (real-world, not toy)
       - Notes/caveats if applicable
       - Related API links

2. **Improve example quality:**
   - Replace toy examples with real-world patterns
   - For complex examples, consider VitePress transclusion:
     - Create test file in packages/core/tests/examples/
     - Reference in docs: `<<< @/../packages/core/tests/examples/example-name.ts`
   - Ensure all examples follow build-time constraint

3. **Run complete verification pipeline (sequential):**
   ```bash
   # 1. ESLint (structural)
   pnpm lint docs/advanced/api-reference.md
   
   # 2. Link checker
   bash scripts/check-links.sh
   
   # 3. API verifier
   bash scripts/verify-api-docs.sh
   
   # 4. Placeholder detection
   bash scripts/check-placeholders.sh
   ```

4. **Fix all failures until zero issues:**
   - ESLint errors: Fix markdown syntax or code blocks
   - Broken links: Update to valid paths
   - API mismatches: Correct signatures
   - Placeholders: Complete or remove

**Zero tolerance:** All validations must pass.
  </action>
  <verify>
1. `pnpm lint docs/advanced/api-reference.md` exits 0
2. `bash scripts/check-links.sh` reports no broken links
3. `bash scripts/verify-api-docs.sh` reports:
   - 0 signature mismatches for @pikacss/core
   - 0 contradictions
   - Coverage >= 80% for core public APIs
4. `bash scripts/check-placeholders.sh` finds no placeholders
5. All @pikacss/core examples follow build-time constraint
  </verify>
  <done>
- All public @pikacss/core APIs documented (80%+ coverage)
- All examples are real-world and validated
- Complete verification pipeline passes with zero failures
- API reference provides comprehensive documentation for core package
  </done>
</task>

</tasks>

<verification>
**File completion criteria:**
- API verifier reports 0 signature mismatches for @pikacss/core in api-reference.md
- API coverage >= 80% for core public APIs
- 0 contradictions across documentation files
- Complete verification pipeline passes

**Success signals:**
- `bash scripts/verify-api-docs.sh` shows zero issues for core APIs in api-reference.md
- Every documented signature matches TypeScript Compiler API extraction
- All examples compile and follow PikaCSS constraints
</verification>

<success_criteria>
1. All @pikacss/core API signatures in api-reference.md match actual implementation
2. Engine class fully documented with all public methods
3. EngineConfig interface completely documented with grouped options
4. Plugin system API accurately documented
5. API coverage >= 80% for @pikacss/core public APIs
6. Complete verification pipeline passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-package-correction-pikacss-core/04-03-SUMMARY.md`
</output>
