---
phase: 04-core-package-correction-pikacss-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
autonomous: true

must_haves:
  truths:
    - "AGENTS.md accurately describes @pikacss/core package structure"
    - "Core package responsibilities match actual implementation"
    - "Layered architecture diagram reflects real package organization"
    - "No contradictions between AGENTS.md and source code"
  artifacts:
    - path: "AGENTS.md"
      provides: "Accurate core package architecture section"
      min_lines: 677
      sections: ["Package Responsibilities", "Layered Package Architecture", "Package Dependencies"]
  key_links:
    - from: "AGENTS.md"
      to: "packages/core/src/index.ts"
      via: "exports verification"
      pattern: "export.*from"
    - from: "AGENTS.md"
      to: "packages/core/package.json"
      via: "dependency verification"
      pattern: '"dependencies"'
---

<objective>
Correct AGENTS.md core package architecture section to match actual @pikacss/core implementation.

Purpose: Establish correct mental model of @pikacss/core structure before correcting detailed API documentation. AGENTS.md serves as architectural foundation for all subsequent documentation.

Output: AGENTS.md with accurate @pikacss/core descriptions passing all verification checks.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-package-correction-pikacss-core/04-CONTEXT.md
@.planning/phases/04-core-package-correction-pikacss-core/04-RESEARCH.md

# Core package source for verification
@packages/core/src/index.ts
@packages/core/package.json
@packages/core/README.md

# Verification infrastructure
@packages/api-verifier/src/extractor.ts
@scripts/verify-api-docs.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract actual @pikacss/core structure and verify AGENTS.md accuracy</name>
  <files>AGENTS.md</files>
  <action>
1. **Extract actual core package information:**
   - Read `packages/core/src/index.ts` to identify all public exports
   - Read `packages/core/package.json` to verify dependencies (should only have csstype)
   - Read `packages/core/src/internal/*.ts` to understand internal structure
   - Use API verifier to extract complete type signatures: `node packages/api-verifier/dist/cli.mjs --package core`

2. **Compare AGENTS.md descriptions with reality:**
   - Verify "Package Responsibilities" section (line ~70-90) matches actual exports
   - Verify layered architecture diagram (line ~50-80) shows correct dependencies
   - Check that core package is described as having "0 dependencies (only csstype)" 
   - Validate package dependencies table (line ~580-590) shows accurate counts

3. **Fix inaccuracies found:**
   - Update package responsibility descriptions to match actual implementation
   - Correct any hallucinated features or APIs not present in source
   - Update dependency counts if incorrect
   - Fix any contradictions with source code

4. **Apply critical-first approach:**
   - Fix signature mismatches and contradictions first (blockers)
   - Then address missing coverage or vague descriptions (warnings)

**What to avoid:**
- Don't add implementation details that belong in API reference docs
- Don't describe internal utilities (prefixed with `_` or in `/internal/`)
- Don't modify code to match documentation (doc corrects to match code)
  </action>
  <verify>
1. Run API verifier: `bash scripts/verify-api-docs.sh` - check for contradictions in AGENTS.md
2. Verify exports match: Compare AGENTS.md exports list with `grep "export" packages/core/src/index.ts`
3. Verify dependencies: Compare dependency count with `cat packages/core/package.json | grep -A5 '"dependencies"'`
4. ESLint validation: `pnpm lint AGENTS.md` - no structural errors
  </verify>
  <done>
- AGENTS.md "Package Responsibilities" section accurately describes @pikacss/core
- Layered architecture diagram shows correct dependency relationships
- Package dependencies table shows accurate counts
- No contradictions between AGENTS.md and actual source code
- API verifier reports zero contradictions for core package in AGENTS.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate cross-references and run full verification</name>
  <files>AGENTS.md</files>
  <action>
1. **Validate all @pikacss/core cross-references:**
   - Search for all mentions of core package APIs in AGENTS.md: `grep -n "@pikacss/core\|Engine\|pika()\|defineEnginePlugin" AGENTS.md`
   - For each API mentioned, verify it exists in actual exports
   - Check that usage examples are consistent with actual API signatures

2. **Run complete verification pipeline:**
   - ESLint: `pnpm lint AGENTS.md`
   - Link checker: `bash scripts/check-links.sh`
   - API verifier: `bash scripts/verify-api-docs.sh`
   - Check for placeholders: `bash scripts/check-placeholders.sh`

3. **Fix any issues found:**
   - Broken references to core APIs
   - Inconsistent terminology
   - Outdated examples

**Verification order (sequential):**
1. ESLint (structural)
2. Link checker
3. API verifier
4. Placeholder detection

**Zero tolerance:** All validations must pass before task completion.
  </action>
  <verify>
1. `pnpm lint AGENTS.md` exits 0
2. `bash scripts/check-links.sh` reports no broken links in AGENTS.md
3. `bash scripts/verify-api-docs.sh` reports zero contradictions for core APIs in AGENTS.md
4. `bash scripts/check-placeholders.sh` finds no placeholders in AGENTS.md
5. Manual spot-check: Compare 3 random core API mentions in AGENTS.md with actual source
  </verify>
  <done>
- All validations pass with zero failures
- All core package API references in AGENTS.md are accurate
- No broken links, placeholders, or structural errors
- AGENTS.md establishes correct architectural foundation for Phase 4
  </done>
</task>

</tasks>

<verification>
**File completion criteria:**
- API verifier reports 0 contradictions for @pikacss/core in AGENTS.md
- All cross-references to core APIs validated against source
- Complete verification pipeline passes (ESLint, links, API, placeholders)

**Success signals:**
- `bash scripts/verify-api-docs.sh` shows zero issues for AGENTS.md core sections
- Package responsibilities match actual exports from `packages/core/src/index.ts`
- Architecture diagram reflects real monorepo structure
</verification>

<success_criteria>
1. AGENTS.md "Package Responsibilities" section for @pikacss/core matches actual implementation
2. Layered architecture diagram accurately shows core as foundation layer with zero dependencies
3. All core API mentions in AGENTS.md validated against source code
4. Complete verification pipeline passes with zero failures
5. No contradictions between AGENTS.md and packages/core/src/* detected by API verifier
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-package-correction-pikacss-core/04-01-SUMMARY.md`
</output>
