---
phase: 10-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/eslint-config/package.json
  - packages/eslint-config/tsdown.config.ts
  - packages/eslint-config/src/index.ts
  - tsconfig.json
autonomous: true
must_haves:
  truths:
    - "Package @pikacss/eslint-config exists in workspace"
    - "pnpm build generates dist/ artifacts"
    - "package.json has correct peerDependencies"
  artifacts:
    - path: "packages/eslint-config/package.json"
      provides: "Package configuration"
      contains: '"name": "@pikacss/eslint-config"'
    - path: "packages/eslint-config/dist/index.mjs"
      provides: "ESM build output"
      min_lines: 1
  key_links:
    - from: "packages/eslint-config/package.json"
      to: "dist/index.mjs"
      via: "exports"
      pattern: '"default": "./dist/index.mjs"'
---

<objective>
Initialize the @pikacss/eslint-config package using standard scaffolding tools and configure it for ESLint 9 compatibility.

Purpose: Establish the shared configuration package structure to enable enforcing build-time constraints across the monorepo and for users.
Output: A buildable package with correct dependencies and exports.
</objective>

<execution_context>
@/Users/deviltea/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/deviltea/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/newpkg.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold package</name>
  <files>packages/eslint-config/package.json</files>
  <action>
    Run `pnpm newpkg eslint-config` to create the package structure.
    This uses the internal script to generate package.json, tsconfig, and tsdown config.
  </action>
  <verify>ls packages/eslint-config/package.json</verify>
  <done>Package directory and files exist.</done>
</task>

<task type="auto">
  <name>Task 2: Configure dependencies</name>
  <files>packages/eslint-config/package.json</files>
  <action>
    Install required dependencies for ESLint 9+ configuration:
    1. Add `eslint` as a peer dependency (version ^9.0.0).
    2. Add `eslint`, `@types/eslint`, and `@typescript-eslint/utils` as dev dependencies (or workspace deps) to ensure type safety during development.
    
    Use:
    `pnpm add --save-peer "eslint@^9.0.0" --filter @pikacss/eslint-config`
    `pnpm add -D "eslint@^9.0.0" "@types/eslint" "@typescript-eslint/utils" --filter @pikacss/eslint-config`
  </action>
  <verify>grep "eslint" packages/eslint-config/package.json</verify>
  <done>package.json contains correct peerDependencies and devDependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize source and verify build</name>
  <files>packages/eslint-config/src/index.ts</files>
  <action>
    1. Update `packages/eslint-config/src/index.ts` to export an empty array (valid Flat Config placeholder).
       Content: `export default []`
    2. Run `pnpm build --filter @pikacss/eslint-config` to verify the build pipeline.
    3. Check that `dist/` contains `index.mjs` and `index.d.mts`.
  </action>
  <verify>ls packages/eslint-config/dist/index.mjs && ls packages/eslint-config/dist/index.d.mts</verify>
  <done>Build succeeds and artifacts are generated.</done>
</task>

</tasks>

<verification>
# Verification Steps
1. Run `pnpm install` to ensure workspace linking.
2. Run `pnpm build --filter @pikacss/eslint-config`.
3. Check `dist/index.mjs` exists.
4. Check `package.json` exports match the dist files.
</verification>

<success_criteria>
- [ ] Package @pikacss/eslint-config builds successfully
- [ ] Exports are configured for hybrid/dist usage (standard dist for now)
- [ ] Peer dependencies include eslint >= 9.0.0
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-01-SUMMARY.md`
</output>
