---
wave: 1
depends_on: []
autonomous: true
files_modified:
  - packages/eslint-config/src/rules/pika-build-time.ts
  - packages/eslint-config/src/configs/recommended.ts
  - packages/eslint-config/src/plugin.ts
  - packages/eslint-config/src/index.ts
  - packages/eslint-config/package.json
  - packages/eslint-config/tests/config.test.ts
must_haves:
  - Exported "recommended" config compatible with ESLint Flat Config
  - "pika-build-time" rule enabled by default in recommended config
  - No runtime dependencies in package.json (move utils to devDeps or remove)
  - Integration tests passing for the exported config
---

# Phase 12: Configuration Implementation

**Goal:** Create a consumable ESLint Flat Config preset that bundles the `pika-build-time` rule, ensuring a zero-dependency runtime for end users.

## Tasks

<task>
<id>1.1</id>
<action>Refactor `packages/eslint-config/src/rules/pika-build-time.ts` to remove runtime dependency on `@typescript-eslint/utils`.</action>
<description>
- Replace `ESLintUtils.RuleCreator` with a simple object factory or standard rule definition.
- Replace `AST_NODE_TYPES` enums with string literals (e.g., "CallExpression", "Literal").
- Ensure type safety is maintained using `import type` from `@typescript-eslint/utils` (dev dependency only).
- Verify unit tests still pass after refactor.
</description>
</task>

<task>
<id>1.2</id>
<action>Create `packages/eslint-config/src/plugin.ts` to define the plugin object.</action>
<description>
- Import the refactored rule.
- Define the plugin object with `rules: { 'pika-build-time': rule }`.
- Add plugin metadata (name, version).
</description>
</task>

<task>
<id>1.3</id>
<action>Create `packages/eslint-config/src/configs/recommended.ts`.</action>
<description>
- Import the plugin from `./plugin`.
- Export a `recommended` configuration object (or array) compatible with ESLint Flat Config.
- Register the plugin under the namespace `pika`.
- Enable the rule `'pika/pika-build-time': 'error'`.
</description>
</task>

<task>
<id>1.4</id>
<action>Update `packages/eslint-config/src/index.ts` and `package.json`.</action>
<description>
- `src/index.ts`: Export the plugin as default and `configs` as a named export.
- `package.json`: Move `@typescript-eslint/utils` to `devDependencies`. Ensure `main`, `module`, and `exports` are correctly configured for the build output.
</description>
</task>

<task>
<id>1.5</id>
<action>Create integration tests in `packages/eslint-config/tests/config.test.ts`.</action>
<description>
- Use the `ESLint` class to load the configuration.
- Verify that `pika/pika-build-time` rule is active and reports errors on invalid code.
- Ensure the config works without explicit parser configuration (inheriting from environment/test setup).
</description>
</task>

<task>
<id>1.6</id>
<action>Verify build and final artifacts.</action>
<description>
- Run `pnpm build`.
- Check `dist/` output for correctness (ESM/CJS).
- Run `pnpm test` to ensure all tests (unit + integration) pass.
</description>
</task>

## Verification

1.  **Zero Dependencies:** Check `packages/eslint-config/package.json` `dependencies` section is empty.
2.  **Config Export:** Inspect `dist/index.mjs` (or similar) to verify `configs.recommended` is exported.
3.  **Rule Activation:** Integration test confirms `pika()` calls with dynamic arguments trigger an error when using the recommended config.
