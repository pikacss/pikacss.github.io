---
phase: 01-foundation-verification-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .github/workflows/ci.yml
autonomous: false

must_haves:
  truths:
    - "All internal markdown links resolve to existing files or headings"
    - "All external links return valid HTTP responses (200, 301, 302)"
    - "Link validation runs automatically in CI pipeline"
    - "Developers receive immediate feedback on broken links"
  artifacts:
    - path: "package.json"
      provides: "Link checking scripts"
      contains: "markdown-link-check"
    - path: ".github/workflows/ci.yml"
      provides: "CI integration for link validation"
      contains: "markdown-link-check"
  key_links:
    - from: "package.json scripts"
      to: "markdown-link-check CLI"
      via: "npm script execution"
      pattern: "markdown-link-check"
    - from: ".github/workflows/ci.yml"
      to: "link validation script"
      via: "CI job step"
      pattern: "pnpm.*check-links"
---

<objective>
Establish automated link validation for all markdown files with CI integration.

Purpose: Ensure all documentation links (internal file references and external URLs) remain valid, preventing broken navigation and outdated references.

Output: Working link validation system integrated into CI pipeline with clear reporting of broken links.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-verification-infrastructure/01-CONTEXT.md
@package.json
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Set up markdown-link-check for link validation</name>
  <files>package.json</files>
  <action>
Install markdown-link-check for validating internal and external links:
```bash
pnpm add -D -w markdown-link-check
```

Add validation scripts to root package.json:
```json
{
  "scripts": {
    "check-links": "markdown-link-check --config .markdown-link-check.json --quiet $(find . -name '*.md' ! -path './node_modules/*' ! -path './.planning/*')",
    "check-links:verbose": "markdown-link-check --config .markdown-link-check.json $(find . -name '*.md' ! -path './node_modules/*' ! -path './.planning/*')"
  }
}
```

Create `.markdown-link-check.json` configuration:
```json
{
  "ignorePatterns": [
    {
      "pattern": "^https://github.com/.*/pull/"
    },
    {
      "pattern": "^https://github.com/.*/issues/"
    }
  ],
  "timeout": "10s",
  "retryOn429": true,
  "retryCount": 3,
  "aliveStatusCodes": [200, 206, 301, 302, 304]
}
```

Rationale:
- Ignore PR/issue links (often private or ephemeral)
- Retry on rate limiting (429 responses common for external APIs)
- Accept redirect status codes (301, 302) as valid
- Timeout prevents hanging on slow external sites
- Exclude node_modules and .planning directories
  </action>
  <verify>
Run link check on a small subset to test configuration:
```bash
pnpm check-links:verbose README.md
```

Should report:
- Internal links validated against file existence
- External links checked with HTTP requests
- Clear output showing passed/failed links
  </verify>
  <done>
- markdown-link-check installed in package.json devDependencies
- check-links scripts added to package.json
- .markdown-link-check.json configuration created
- Link validation runs successfully on sample files
  </done>
</task>

<task type="auto">
  <name>Integrate link validation into CI pipeline</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Add link validation job to existing CI workflow at `.github/workflows/ci.yml`:

If no ci.yml exists, create one. If it exists, add a new job:

```yaml
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  # ... existing jobs ...
  
  link-validation:
    name: Validate Documentation Links
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
      
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      
      - name: Check documentation links
        run: pnpm check-links
```

Job runs on every push and PR, failing the build if broken links detected.

Note: If ci.yml already has comprehensive steps, integrate link checking as an additional step in an existing job rather than creating a separate job (to save CI minutes).
  </action>
  <verify>
Check CI workflow syntax:
```bash
cat .github/workflows/ci.yml
```

Verify job structure is valid YAML and includes link-validation steps.
  </verify>
  <done>
- Link validation integrated into CI workflow
- CI fails on broken links (exit code 1 from markdown-link-check)
- Developers receive immediate feedback on PRs
- Link validation runs automatically on every commit to main branches
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete link validation infrastructure with ESLint markdown linting and markdown-link-check integration.

Automated validation covers:
1. ESLint markdown structural checks (from Plan 01)
2. Internal link validation (file references, heading anchors)
3. External link validation (HTTP status checks)
4. CI pipeline integration (pre-commit hooks + GitHub Actions)
  </what-built>
  
  <how-to-verify>
1. **Test ESLint markdown linting:**
   ```bash
   pnpm eslint README.md
   ```
   Expected: No false positives from code blocks

2. **Test link validation locally:**
   ```bash
   pnpm check-links:verbose
   ```
   Expected: Reports on all links (internal + external), identifies any broken links

3. **Check pre-commit hooks:**
   ```bash
   git add README.md
   git commit -m "test: verify pre-commit linting"
   ```
   Expected: ESLint runs on markdown files before commit completes

4. **Verify CI integration:**
   Check `.github/workflows/ci.yml` includes link-validation job or step

5. **Review baseline issues:**
   Check Plan 01 summary for documented structural issues (if any)

**Success indicators:**
- ESLint processes markdown without code block errors
- Link checker identifies broken links with file:line locations
- Pre-commit hooks prevent committing unlinted markdown
- CI pipeline fails on broken links
  </how-to-verify>
  
  <resume-signal>
Type "approved" if verification infrastructure is working, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
1. Run `pnpm check-links` and verify it scans all 73 markdown files
2. Check `.github/workflows/ci.yml` contains link validation job/step
3. Test pre-commit hooks trigger ESLint on markdown changes
4. Verify broken links (if any) are reported with clear file:line context
</verification>

<success_criteria>
- [ ] markdown-link-check installed and configured
- [ ] Link validation scripts added to package.json
- [ ] CI pipeline includes link validation job
- [ ] Link checker reports internal and external link status
- [ ] Broken links (if any) documented in baseline
- [ ] Human verification confirms infrastructure working end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-verification-infrastructure/01-02-SUMMARY.md`
</output>
