---
phase: 01-foundation-verification-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [eslint.config.mjs]
autonomous: true

must_haves:
  truths:
    - "ESLint validates markdown files without false positives from code blocks"
    - "Pre-commit hooks run markdown validation automatically"
    - "Markdown syntax errors are caught before commit"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Markdown validation enabled via @deviltea/eslint-config"
      contains: "removed markdown ignores"
  key_links:
    - from: "eslint.config.mjs"
      to: "@deviltea/eslint-config"
      via: "inherited markdown rules"
      pattern: "@deviltea/eslint-config"
    - from: "lint-staged"
      to: "eslint --fix"
      via: "pre-commit hook"
      pattern: "eslint --fix"
---

<objective>
Enable ESLint markdown validation across all 73 documentation files by removing ignores from eslint.config.mjs.

Purpose: Establish structural validation baseline that catches markdown syntax errors, leveraging existing @deviltea/eslint-config which already includes @antfu/eslint-config's built-in markdown support.

Output: ESLint validating markdown files with proper code block handling, no false positives, integrated with pre-commit hooks.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-verification-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-verification-infrastructure/01-RESEARCH.md
@eslint.config.mjs
@package.json
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Remove markdown ignores from ESLint configuration</name>
  <files>eslint.config.mjs</files>
  <action>
Remove ALL markdown ignore patterns from the `ignores` array in eslint.config.mjs:
- `'**/README.md'`
- `'docs/**/*.md'`
- `'.github/skills/**/*.md'`
- `'**/AGENTS.md'`

IMPORTANT: Keep `.planning/**` ignore (planning docs should remain ignored).

The @deviltea/eslint-config already inherits @antfu/eslint-config which includes markdown linting support that properly handles code blocks. No additional packages or configuration needed - just remove the ignores to activate validation.

DO NOT add any new plugins or configuration. The existing config already has what we need.
  </action>
  <verify>
Run `pnpm lint` and confirm:
1. ESLint processes markdown files (shows "Linting..." for .md files)
2. No false positives from code blocks (ellipsis syntax, incomplete examples)
3. Pre-commit hook triggers markdown validation (test by staging a markdown file with intentional error)
  </verify>
  <done>
- All markdown ignore patterns removed except `.planning/**`
- `pnpm lint` successfully validates markdown files
- Pre-commit hooks enforce markdown validation
- No false positives from code block content
  </done>
</task>

</tasks>

<verification>
**Structural validation checks:**
1. Run `pnpm lint` on clean codebase - should pass without errors
2. Introduce intentional markdown error (e.g., broken list syntax) - should be caught
3. Stage markdown file with error - pre-commit hook should reject commit
4. Verify code blocks with `...` ellipsis don't cause false positives
5. Check all 73 markdown files are now being validated (not ignored)

**Quality baseline:**
- Count current markdown issues by running ESLint and collecting output
- Document issue distribution across file types (README, docs/, skills/, AGENTS.md)
</verification>

<success_criteria>
1. ✅ eslint.config.mjs has no markdown file ignores (except .planning/**)
2. ✅ `pnpm lint` validates all 73 markdown files
3. ✅ Code blocks with ellipsis syntax don't trigger false positives
4. ✅ Pre-commit hooks enforce markdown validation
5. ✅ ESLint reports zero or minimal markdown structural issues
6. ✅ No breaking changes to existing lint workflow
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-verification-infrastructure/01-01-SUMMARY.md` with:
- Configuration changes made
- Number of markdown files now validated
- Any issues discovered during activation
- Pre-commit hook verification results
</output>
