---
phase: 01-foundation-verification-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.mjs
  - package.json
autonomous: true

must_haves:
  truths:
    - "ESLint processes markdown files without treating code blocks as standalone files"
    - "Markdown linting rules detect structural issues (broken links placeholders, inconsistent formatting)"
    - "ESLint runs on all 73 markdown files without false positives"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Markdown linting configuration"
      contains: "markdown"
    - path: "package.json"
      provides: "ESLint markdown plugin dependency"
      contains: "@eslint/markdown"
  key_links:
    - from: "eslint.config.mjs"
      to: "@eslint/markdown plugin"
      via: "plugin import and configuration"
      pattern: "@eslint/markdown"
    - from: "lint-staged"
      to: "markdown files"
      via: "ESLint execution on commit"
      pattern: "eslint --fix"
---

<objective>
Configure ESLint to validate markdown files' structural integrity without false positives from code blocks.

Purpose: Enable automated markdown quality checks that integrate with existing pre-commit hooks, establishing the foundation for documentation verification.

Output: Working ESLint markdown configuration that validates all 73 markdown files in pre-commit pipeline.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-verification-infrastructure/01-CONTEXT.md
@eslint.config.mjs
@package.json
</context>

<tasks>

<task type="auto">
  <name>Install and configure ESLint markdown plugin</name>
  <files>package.json, eslint.config.mjs</files>
  <action>
Install @eslint/markdown plugin to root package.json:
```bash
pnpm add -D -w @eslint/markdown
```

Update eslint.config.mjs to:
1. Import markdown plugin: `import markdown from '@eslint/markdown'`
2. Add markdown configuration that:
   - Processes markdown files (*.md pattern)
   - Enables markdown/no-missing-label-refs (detects broken internal links)
   - Enables markdown/no-duplicate-headings (ensures unique heading IDs)
   - Does NOT lint code blocks as JavaScript/TypeScript (processor handles this)
3. Remove all markdown file ignores currently in the config:
   - Remove `**/README.md`
   - Remove `docs/**/*.md`
   - Remove `.github/skills/**/*.md`
   - Remove `**/AGENTS.md`

Keep `.planning/**` ignored (planning docs are not part of the 73 target files).

Configuration approach:
- Use markdown.configs.recommended as base
- Customize with project-specific rules
- Ensure code blocks are treated as content, not linted code

Reference: @eslint/markdown processes markdown AST and ignores code block internals by default.

Verify lint-staged configuration:
- Check that lint-staged config in package.json or separate config file includes markdown files in the glob pattern
- Typical pattern: `"*.md": "eslint --fix"` or `"*": "eslint --fix"`
- If markdown files not covered, add them explicitly
  </action>
  <verify>
Run ESLint on a sample markdown file to confirm no false positives:
```bash
pnpm eslint README.md
```

Should pass without errors related to code block syntax (ellipsis, incomplete snippets).

Verify lint-staged includes markdown:
```bash
cat package.json | grep -A 10 "lint-staged"
```

Should show markdown files in the pattern.
  </verify>
  <done>
- @eslint/markdown installed in package.json devDependencies
- eslint.config.mjs imports and configures markdown plugin
- All markdown file ignores removed from eslint.config.mjs
- ESLint runs on markdown files without code block false positives
- lint-staged configuration verified to include markdown files
  </done>
</task>

<task type="auto">
  <name>Validate markdown linting across all target files</name>
  <files>None (verification task)</files>
  <action>
Run ESLint on all 73 target markdown files to establish baseline:

```bash
# Test all markdown categories
pnpm eslint "**/*.md" --max-warnings 0
```

Expected outcome:
- ESLint processes all markdown files
- Reports real structural issues (if any):
  - Missing label references (broken internal links)
  - Duplicate headings (conflicting IDs)
  - Inconsistent formatting (if rules detect it)
- Does NOT report false positives from code blocks

If ESLint reports structural issues (broken links, etc.), DO NOT fix them in this task. This task only validates that the linting infrastructure works. Document any found issues in the summary for Phase 2+ to address.

If ESLint still produces code block false positives, adjust markdown configuration to disable problematic rules while keeping structural checks.
  </action>
  <verify>
```bash
pnpm eslint "**/*.md"
```

Exit code 0 indicates full compliance. Exit code 1 with error output indicates real structural issues found (expected baseline).

Acceptable outcomes:
- Exit 0: All files pass (ideal but unlikely for 73 files)
- Exit 1 with structural errors: Document errors in summary as baseline

Unacceptable outcome:
- Code block syntax errors (ellipsis, incomplete code) causing failures
  </verify>
  <done>
- ESLint successfully processes all 73 markdown files
- No false positives from code blocks (ellipsis, incomplete examples)
- Baseline of structural issues identified and documented
- Markdown linting integrated into pre-commit hooks via existing lint-staged config
  </done>
</task>

</tasks>

<verification>
1. Run `pnpm eslint "**/*.md"` and verify it completes without code block false positives
2. Check that `eslint.config.mjs` no longer contains markdown file ignores
3. Verify @eslint/markdown is in package.json devDependencies
4. Confirm pre-commit hooks will run markdown linting (lint-staged already configured for `"*": "eslint --fix"`)
</verification>

<success_criteria>
- [ ] @eslint/markdown installed and configured
- [ ] All 73 markdown files processed by ESLint
- [ ] Zero code block false positives (ellipsis, incomplete snippets don't trigger errors)
- [ ] Structural validation rules active (broken links, duplicate headings detected)
- [ ] Pre-commit hooks enforce markdown linting via existing lint-staged setup
- [ ] Baseline of real issues documented for future phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-verification-infrastructure/01-01-SUMMARY.md`
</output>
