---
phase: 06-plugin-system-correction
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - docs/advanced/plugin-development.md
  - packages/api-verifier/tests/docs/plugin-development.test.ts
autonomous: true

must_haves:
  truths:
    - "Plugin development guide module augmentation examples compile correctly"
    - "All documented plugin hooks exist in @pikacss/core"
    - "Plugin execution order documented matches implementation"
    - "Code examples follow patterns from official plugins"
  artifacts:
    - path: "docs/advanced/plugin-development.md"
      provides: "Accurate plugin development guide"
      min_lines: 200
    - path: "packages/api-verifier/tests/docs/plugin-development.test.ts"
      provides: "API verification for plugin development guide"
      min_lines: 60
  key_links:
    - from: "docs/advanced/plugin-development.md"
      to: "@pikacss/core EnginePlugin"
      via: "documented hooks match actual interface"
      pattern: "defineEnginePlugin.*hooks"
    - from: "docs/advanced/plugin-development.md"
      to: "packages/plugin-*/src/index.ts"
      via: "examples follow official plugin patterns"
      pattern: "module augmentation.*EngineConfig"
---

<objective>
Correct docs/advanced/plugin-development.md using verified patterns from official plugins.

Purpose: Ensure plugin development guide accurately reflects @pikacss/core plugin system, uses verified patterns from plugin-reset/typography/icons, and all examples compile correctly. This completes plugin documentation by providing accurate guidance for custom plugin creation.

Output: Verified plugin development guide with comprehensive examples and API verification.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plugin-system-correction/06-RESEARCH.md

# Prior plans from this phase (verified plugin patterns)
@.planning/phases/06-plugin-system-correction/06-01-SUMMARY.md
@.planning/phases/06-plugin-system-correction/06-02-SUMMARY.md
@.planning/phases/06-plugin-system-correction/06-03-SUMMARY.md

# Phase 3 API verification
@.planning/phases/03-api-verification-system/03-04-SUMMARY.md

# Source files
@packages/core/src/types/plugin.ts
@packages/plugin-reset/src/index.ts
@packages/plugin-typography/src/index.ts
@packages/plugin-icons/src/index.ts
@docs/advanced/plugin-development.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and correct plugin-development.md guide</name>
  <files>
    docs/advanced/plugin-development.md
  </files>
  <action>
    Use Phase 3 API verifier to extract from packages/core/src/types/plugin.ts:
    1. EnginePlugin interface definition
    2. All available plugin hooks (configureEngine, transformStyleDefinitions, etc.)
    3. Plugin order values ('pre', 'post', undefined)
    4. defineEnginePlugin helper signature
    
    Compare against docs/advanced/plugin-development.md:
    - Plugin Anatomy section: verify defineEnginePlugin usage matches actual API
    - Plugin Execution Order: verify 'pre'/'post'/undefined values match implementation
    - Available hooks: verify all documented hooks exist in EnginePlugin interface
    - Module augmentation examples: verify follow verified patterns from official plugins
    
    Corrections needed:
    - Update hook signatures to match actual EnginePlugin interface
    - Add comprehensive module augmentation section using verified patterns from 06-01/02/03:
      * Show complete example with declare module + implementation
      * Reference official plugins (reset, typography, icons) as examples
      * Explain when and why to augment EngineConfig
    - Verify plugin order documentation matches implementation
    - Update examples to follow official plugin patterns:
      * Use defineEnginePlugin correctly
      * Show configureRawConfig hook for reading augmented config
      * Show configureEngine hook for registering shortcuts/preflights
      * Demonstrate transformStyleDefinitions for custom properties
    - Add section on testing plugins:
      * Functional tests (runtime behavior)
      * Type tests (module augmentation verification)
      * Reference type assertion patterns from 06-01/02/03
    - Ensure all code examples compile successfully
    
    Cross-reference with official plugins:
    - plugin-reset: Simple plugin with module augmentation (enum config)
    - plugin-typography: Medium complexity with variables configuration
    - plugin-icons: Complex plugin with multiple config options
  </action>
  <verify>
    1. Run API verifier: `pnpm --filter @pikacss/api-verifier test tests/docs/plugin-development.test.ts`
    2. Extract and compile all code examples from plugin-development.md
    3. Verify plugin hooks match @pikacss/core types: grep -A 20 "interface EnginePlugin" packages/core/src/types/plugin.ts
  </verify>
  <done>
    - All plugin hooks documented match EnginePlugin interface
    - Module augmentation section complete with verified patterns
    - Plugin examples follow official plugin structure
    - Execution order documentation matches implementation
    - All code examples compile successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive API verification test for plugin guide</name>
  <files>
    packages/api-verifier/tests/docs/plugin-development.test.ts
  </files>
  <action>
    Create API verification test validating plugin-development.md against @pikacss/core implementation.
    
    Test structure:
    ```typescript
    import * as fs from 'node:fs'
    import * as path from 'node:path'
    import { describe, expect, it } from 'vitest'
    
    describe('plugin-development.md API documentation', () => {
      const docPath = path.join(__dirname, '../../../docs/advanced/plugin-development.md')
      const pluginTypesPath = path.join(__dirname, '../../../packages/core/src/types/plugin.ts')
      
      it('should document defineEnginePlugin helper', () => {
        const doc = fs.readFileSync(docPath, 'utf-8')
        const source = fs.readFileSync(pluginTypesPath, 'utf-8')
        
        expect(doc).toContain('defineEnginePlugin')
        expect(source).toMatch(/export.*defineEnginePlugin/)
      })
      
      it('should document all valid plugin order values', () => {
        const doc = fs.readFileSync(docPath, 'utf-8')
        const source = fs.readFileSync(pluginTypesPath, 'utf-8')
        
        // Order: 'pre' | 'post' | undefined
        expect(doc).toContain("'pre'")
        expect(doc).toContain("'post'")
        expect(doc).toContain('undefined')
        
        // Verify in source
        expect(source).toMatch(/order.*'pre'.*'post'/)
      })
      
      it('should document all EnginePlugin hooks', () => {
        const doc = fs.readFileSync(docPath, 'utf-8')
        const source = fs.readFileSync(pluginTypesPath, 'utf-8')
        
        // Extract interface EnginePlugin from source
        const interfaceMatch = source.match(/interface EnginePlugin\s*{([^}]+)}/s)
        expect(interfaceMatch).toBeDefined()
        
        const hooks = interfaceMatch![1]
          .match(/(\w+)\??:/g)
          ?.map(h => h.replace(/\??:/, '')) || []
        
        // Common hooks that should be documented
        const commonHooks = ['configureEngine', 'transformStyleDefinitions', 'configureRawConfig']
        
        commonHooks.forEach(hook => {
          if (hooks.includes(hook)) {
            // If hook exists in interface, must be documented
            expect(doc).toContain(hook)
          }
        })
      })
      
      it('should show module augmentation pattern', () => {
        const doc = fs.readFileSync(docPath, 'utf-8')
        
        expect(doc).toContain('declare module')
        expect(doc).toContain('@pikacss/core')
        expect(doc).toContain('EngineConfig')
      })
      
      it('should reference official plugins as examples', () => {
        const doc = fs.readFileSync(docPath, 'utf-8')
        
        // Should reference at least one official plugin
        const officialPlugins = ['plugin-reset', 'plugin-typography', 'plugin-icons']
        const referencesPlugin = officialPlugins.some(plugin => doc.includes(plugin))
        
        expect(referencesPlugin).toBe(true)
      })
      
      it('should document plugin testing approaches', () => {
        const doc = fs.readFileSync(docPath, 'utf-8')
        
        // Should mention testing
        expect(doc.toLowerCase()).toMatch(/test/i)
        
        // Should cover type testing or module augmentation verification
        const mentionsTypeTesting = doc.includes('expectTypeOf') || 
                                    doc.includes('type test') ||
                                    doc.includes('augmentation')
        expect(mentionsTypeTesting).toBe(true)
      })
      
      it('should have correct code syntax in all examples', () => {
        const doc = fs.readFileSync(docPath, 'utf-8')
        const codeBlocks = doc.match(/```typescript\n([\s\S]*?)\n```/g)
        
        expect(codeBlocks).toBeDefined()
        expect(codeBlocks!.length).toBeGreaterThan(0)
        
        codeBlocks!.forEach(block => {
          // Must use defineEnginePlugin correctly
          if (block.includes('defineEnginePlugin')) {
            expect(block).toMatch(/return\s+defineEnginePlugin\(/)
          }
          
          // Plugin functions must return EnginePlugin
          if (block.match(/export function \w+\(\)/)) {
            expect(block).toContain('EnginePlugin')
          }
        })
      })
      
      it('should show complete plugin workflow (augmentation + implementation)', () => {
        const doc = fs.readFileSync(docPath, 'utf-8')
        
        // Complete workflow includes:
        // 1. Declare module augmentation
        expect(doc).toContain('declare module')
        
        // 2. Export plugin function
        expect(doc).toMatch(/export function \w+\(\)/)
        
        // 3. Use defineEnginePlugin
        expect(doc).toContain('defineEnginePlugin')
        
        // 4. Show usage in config
        expect(doc).toContain('defineEngineConfig')
      })
    })
    ```
  </action>
  <verify>
    Run API verification: `pnpm --filter @pikacss/api-verifier test tests/docs/plugin-development.test.ts`
  </verify>
  <done>
    - API verification test passes
    - All plugin hooks verified against source
    - Module augmentation pattern present
    - Plugin order values correct
    - Testing guidance included
    - Code examples syntactically valid
  </done>
</task>

</tasks>

<verification>
1. API verifier passes: `pnpm --filter @pikacss/api-verifier test tests/docs/plugin-development.test.ts`
2. All code examples compile: extract TypeScript blocks, run tsc --noEmit
3. Plugin hooks cross-referenced: compare documented hooks with packages/core/src/types/plugin.ts
4. Official plugin patterns validated: ensure examples follow plugin-reset/typography/icons patterns
</verification>

<success_criteria>
1. Plugin development guide accurately documents all EnginePlugin hooks
2. Module augmentation section uses verified patterns from official plugins
3. Plugin execution order matches implementation
4. All code examples compile and follow official plugin structure
5. Testing guidance included with type assertion patterns
6. API verification test catches any future documentation drift
</success_criteria>

<output>
After completion, create `.planning/phases/06-plugin-system-correction/06-04-SUMMARY.md`
</output>
