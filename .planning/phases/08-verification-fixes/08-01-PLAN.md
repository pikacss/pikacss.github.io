---
phase: 08-verification-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api-verifier/src/cli.ts
  - packages/api-verifier/src/comparator.ts
autonomous: true
must_haves:
  truths:
    - "Verifier scans packages/*/README.md files"
    - "Verifier matches function declarations to arrow signatures"
    - "Verifier ignores internal generation suffixes like $1"
  artifacts:
    - path: "packages/api-verifier/src/comparator.ts"
      provides: "Normalized signature comparison"
    - path: "packages/api-verifier/src/cli.ts"
      provides: "Updated file globbing"
---

<objective>
Enhance api-verifier to scan package READMEs and handle signature variations, reducing false positives.

Purpose: Eliminate false mismatches caused by syntax differences (arrow vs function) and missing files.
Output: Improved cli.ts and comparator.ts in api-verifier package.
</objective>

<execution_context>
@/Users/deviltea/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/deviltea/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-verification-fixes/08-RESEARCH.md
@packages/api-verifier/src/cli.ts
@packages/api-verifier/src/comparator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI globs</name>
  <files>packages/api-verifier/src/cli.ts</files>
  <action>
    Modify packages/api-verifier/src/cli.ts to include `packages/*/README.md` in the default file patterns.
    Ensure it scans both `docs/**/*.md` AND package READMEs.
  </action>
  <verify>
    Run `pnpm build` in api-verifier.
    Run `node dist/cli.mjs --dry-run` (or similar check) to verify it finds README files.
  </verify>
  <done>CLI output lists package READMEs as scanned files.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize signature comparison</name>
  <files>packages/api-verifier/src/comparator.ts</files>
  <action>
    Update `normalizeSignature` (or equivalent) in packages/api-verifier/src/comparator.ts to:
    1. Normalize `function name(args): type` to `(name) => type` format for comparison.
    2. Strip `$\d+` suffixes from type names (e.g., `EngineConfig$1` -> `EngineConfig`).
    3. Ensure whitespace is normalized (collapse multiple spaces).
  </action>
  <verify>
    Run `pnpm test` in api-verifier package.
    Create a temporary test case comparing "function foo(): void" and "(foo) => void" if needed.
  </verify>
  <done>False positives for function declarations and internal aliases are eliminated.</done>
</task>

</tasks>

<verification>
Run `pnpm test` in packages/api-verifier to ensure no regression.
</verification>

<success_criteria>
- [ ] CLI accepts and scans packages/*/README.md
- [ ] Comparator considers "function x()" and "(x) =>" as equivalent
- [ ] Comparator ignores $N suffixes
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-fixes/08-01-SUMMARY.md`
</output>
