---
phase: 13-integration
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - .eslint/rules/pika-build-time.ts
  - .eslint/rules/index.ts
autonomous: true
must_haves:
  truths:
    - "Local pika-build-time implementation is gone"
    - "Local rules index no longer exports pika-build-time"
    - "Linting passes cleanly"
  artifacts:
    - path: ".eslint/rules/index.ts"
      provides: "Local rule registry"
      contains: "export const rules"
  key_links:
    - from: ".eslint/rules/index.ts"
      to: "pika-build-time.ts"
      via: "import/export"
      pattern: "pika-build-time" # Should NOT exist
---

<objective>
Remove the legacy local implementation of the `pika-build-time` rule.

Purpose: Complete the migration to the package-based rule, eliminating duplication.
Output: Cleaned up .eslint/rules directory.
</objective>

<execution_context>
@/Users/deviltea/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/deviltea/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-integration/13-CONTEXT.md
@.planning/phases/13-integration/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove local rule file</name>
  <files>.eslint/rules/pika-build-time.ts</files>
  <action>
    Delete `.eslint/rules/pika-build-time.ts`.
  </action>
  <verify>
    if [ ! -f .eslint/rules/pika-build-time.ts ]; then echo "Deleted"; else exit 1; fi
  </verify>
  <done>
    File is removed from filesystem.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update local rules registry</name>
  <files>.eslint/rules/index.ts</files>
  <action>
    Edit `.eslint/rules/index.ts` to remove the import and export of `pikaBuildTimeRule` (or equivalent name).
    Ensure `pikaPackageBoundariesRule` and `pikaModuleAugmentationRule` remain exported.
  </action>
  <verify>
    ! grep "pika-build-time" .eslint/rules/index.ts
  </verify>
  <done>
    Registry no longer references the deleted rule.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify clean linting</name>
  <files>eslint.config.mjs</files>
  <action>
    Run `pnpm lint` across the repository.
    This confirms that the new configuration (from Plan 01) is working and that removing the local rule didn't break anything.
  </action>
  <verify>
    pnpm lint
  </verify>
  <done>
    Linting passes successfully.
  </done>
</task>

</tasks>

<verification>
Ensure no references to the old rule file remain in the codebase (grep search).
</verification>

<success_criteria>
- [ ] pika-build-time.ts is deleted
- [ ] index.ts is updated
- [ ] pnpm lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration/13-02-SUMMARY.md`
</output>
