---
phase: 13-integration
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - packages/core/src/_verify_lint_failure.ts
autonomous: true
must_haves:
  truths:
    - "New rule pika/pika-build-time is active"
    - "Violations are correctly flagged"
  artifacts: []
  key_links: []
---

<objective>
Verify that the new ESLint rule is correctly enforcing build-time constraints.

Purpose: Confirm the integration is effective and not just silently ignored.
Output: Verification results.
</objective>

<execution_context>
@/Users/deviltea/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/deviltea/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-integration/13-CONTEXT.md
@.planning/phases/13-integration/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify rule enforcement</name>
  <files>packages/core/src/_verify_lint_failure.ts</files>
  <action>
    1. Create a temporary file `packages/core/src/_verify_lint_failure.ts` with content:
       ```typescript
       // @ts-nocheck
       declare const pika: any;
       const dynamic = 'value';
       pika({ color: dynamic });
       ```
    2. Run `pnpm lint packages/core/src/_verify_lint_failure.ts` and capture output.
    3. Assert that the output contains error `pika/pika-build-time` (the new rule ID) OR `pikacss/pika-build-time` (if aliased, but package uses `pika`).
    4. Assert that the command failed (exit code != 0).
    5. Delete the temporary file.
  </action>
  <verify>
    # This verification is part of the action itself (asserting output)
    echo "Verification logic embedded in action"
  </verify>
  <done>
    Rule correctly identifies the violation and reports the expected error ID.
  </done>
</task>

</tasks>

<verification>
The task itself is a verification step.
</verification>

<success_criteria>
- [ ] Rule correctly triggers on violation
- [ ] Error message ID corresponds to the new package rule (`pika/pika-build-time`)
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration/13-03-SUMMARY.md`
</output>
