---
phase: 13-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - eslint.config.mjs
autonomous: true
must_haves:
  truths:
    - "Root package.json has @pikacss/eslint-config as devDependency"
    - "eslint.config.mjs imports from @pikacss/eslint-config"
    - "Package is built and ready for consumption"
  artifacts:
    - path: "packages/eslint-config/dist/index.mjs"
      provides: "Compiled ESLint config"
      min_lines: 1
  key_links:
    - from: "eslint.config.mjs"
      to: "@pikacss/eslint-config"
      via: "import statement"
      pattern: "import.*@pikacss/eslint-config"
---

<objective>
Update the monorepo configuration to consume the new @pikacss/eslint-config package.

Purpose: Enforce consistent linting rules using the published package (dogfooding).
Output: Updated package.json and eslint.config.mjs.
</objective>

<execution_context>
@/Users/deviltea/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/deviltea/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-integration/13-CONTEXT.md
@.planning/phases/13-integration/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add package dependency</name>
  <files>package.json</files>
  <action>
    Add `@pikacss/eslint-config` to the root `package.json` devDependencies with version `workspace:*`.
    Run `pnpm install` to link the workspace package.
  </action>
  <verify>
    grep '"@pikacss/eslint-config": "workspace:\*"' package.json
  </verify>
  <done>
    Dependency is present in package.json and installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the config package</name>
  <files>packages/eslint-config/dist/*</files>
  <action>
    Run `pnpm --filter @pikacss/eslint-config build`.
    This ensures the `dist/` artifacts exist, which are required for the root eslint config to load the package.
  </action>
  <verify>
    test -f packages/eslint-config/dist/index.mjs
  </verify>
  <done>
    Build artifacts exist in packages/eslint-config/dist.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ESLint configuration</name>
  <files>eslint.config.mjs</files>
  <action>
    Modify `eslint.config.mjs` to:
    1. Import the package: `import pikaConfig from '@pikacss/eslint-config'`
    2. Remove the manual configuration for `pikacss/pika-build-time` (leave boundary/augmentation rules).
    3. Append `...pikaConfig.configs.recommended` to the exported configuration array.
    
    IMPORTANT: Do NOT remove the existing `plugins: { pikacss: ... }` block that defines local rules (boundaries/augmentation), as they are still needed. Only remove the `pika-build-time` rule configuration if explicitly listed in the `rules` object.
  </action>
  <verify>
    grep "import.*@pikacss/eslint-config" eslint.config.mjs && grep "\.\.\.pikaConfig\.configs\.recommended" eslint.config.mjs
  </verify>
  <done>
    Configuration imports and uses the new package preset.
  </done>
</task>

</tasks>

<verification>
Run `pnpm lint` to ensure the configuration is valid and loadable.
</verification>

<success_criteria>
- [ ] Root package.json includes dependency
- [ ] eslint.config.mjs uses the new package
- [ ] Linting runs without crashing (rules might be duplicated temporarily, which is fine)
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration/13-01-SUMMARY.md`
</output>
