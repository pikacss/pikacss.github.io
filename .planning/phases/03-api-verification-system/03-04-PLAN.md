---
phase: 03-api-verification-system
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - scripts/verify-api-docs.sh
  - .github/workflows/docs-validation.yml
  - API-VERIFICATION-BASELINE.md
  - packages/api-verifier/package.json
autonomous: true

must_haves:
  truths:
    - "CLI script executes API verification and generates reports"
    - "CI pipeline runs API verification on documentation changes"
    - "API mismatches block PR merges"
    - "Baseline report documents current state of API documentation accuracy"
  artifacts:
    - path: "scripts/verify-api-docs.sh"
      provides: "Bash script executing API verification"
      min_lines: 30
    - path: ".github/workflows/docs-validation.yml"
      provides: "Updated CI workflow including API verification step"
      contains: "verify-api-docs.sh"
    - path: "API-VERIFICATION-BASELINE.md"
      provides: "Baseline report of current API documentation state"
      min_lines: 50
  key_links:
    - from: "scripts/verify-api-docs.sh"
      to: "packages/api-verifier/dist/index.mjs"
      via: "executes verification via node"
      pattern: "node.*api-verifier"
    - from: ".github/workflows/docs-validation.yml"
      to: "scripts/verify-api-docs.sh"
      via: "runs verification in CI"
      pattern: "bash scripts/verify-api-docs.sh"
---

<objective>
Integrate API verification into CI pipeline and establish baseline documentation accuracy.

Purpose: Enforce API documentation accuracy through automated CI checks, blocking merges on mismatches, and provide baseline report tracking current documentation state for improvement measurement.

Output: Operational CI integration with blocking validation, executable CLI script, and comprehensive baseline report documenting all API mismatches and coverage gaps.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-verification-system/03-CONTEXT.md
@.planning/phases/03-api-verification-system/03-RESEARCH.md
@.planning/phases/03-api-verification-system/03-01-SUMMARY.md
@.planning/phases/03-api-verification-system/03-02-SUMMARY.md
@.planning/phases/03-api-verification-system/03-03-SUMMARY.md
@.planning/phases/01-foundation-verification-infrastructure/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI verification script</name>
  <files>
    scripts/verify-api-docs.sh
    packages/api-verifier/package.json
  </files>
  <action>
Create executable bash script for API verification following patterns from check-links.sh:

1. Create scripts/verify-api-docs.sh:
   - Shebang: #!/usr/bin/env bash
   - Set error handling: set -e (exit on error), set -u (undefined vars error)
   - Color codes for output (RED, GREEN, YELLOW, RESET)
   
   - Pre-flight checks:
     * Verify @pikacss/api-verifier is built: check for packages/api-verifier/dist/index.mjs
     * If not built, echo error and suggest: pnpm --filter @pikacss/api-verifier build
     * Verify all packages are built (check for dist/ directories in each package)
     * If any missing, suggest: pnpm build
   
   - Execute verification:
     * Run: node packages/api-verifier/dist/index.mjs
     * Capture exit code
     * Display generated reports (cat api-verification-report.md if exists)
   
   - Exit with captured exit code (non-zero if mismatches found)
   
   - Add execution permission: chmod +x scripts/verify-api-docs.sh

2. Add CLI entry point to api-verifier:
   - Update package.json with bin field: { "bin": { "verify-api": "./dist/cli.js" } }
   - Create src/cli.ts:
     * Import verifyAllPackages and generateReports
     * Execute verification
     * Generate reports to ./api-verification-report.*
     * Exit with code 1 if mismatches or undocumented APIs found
     * Exit with code 0 if all checks pass
   - Update tsconfig to include cli.ts
   - Build to ensure cli.js exists

3. Follow existing validation script patterns (check-links.sh structure)
  </action>
  <verify>
bash scripts/verify-api-docs.sh (run verification)
Exit code is non-zero if mismatches exist (expected on first run)
Reports are generated: ls api-verification-report.json api-verification-report.md
Script output shows colored messages
  </verify>
  <done>
Executable CLI script runs API verification, generates reports, and exits with appropriate code for CI integration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate into CI pipeline</name>
  <files>
    .github/workflows/docs-validation.yml
  </files>
  <action>
Add API verification to existing docs-validation workflow following Phase 1 patterns:

1. Locate "PikaCSS Validation" job in docs-validation.yml (added in Phase 2)

2. Add new step after existing PikaCSS validation steps:
   ```yaml
   - name: Verify API Documentation
     run: bash scripts/verify-api-docs.sh
   ```

3. Step ordering (sequential for fast failure):
   - Structural validation (ESLint, links, placeholders)
   - PikaCSS validation (build-time, package boundaries)
   - API verification (new step - final gate)

4. Ensure step runs after build:
   - Build step should already exist (pnpm build)
   - API verification requires built packages
   - Add comment noting dependency on build completion

5. Upload reports as artifacts on failure:
   ```yaml
   - name: Upload API Verification Reports
     if: failure()
     uses: actions/upload-artifact@v4
     with:
       name: api-verification-reports
       path: |
         api-verification-report.json
         api-verification-report.md
   ```

6. Per CONTEXT.md decision: "Any mismatch fails CI" - script exit code naturally blocks merge

7. Follow existing workflow structure from Phase 1 CI integration
  </action>
  <verify>
git diff .github/workflows/docs-validation.yml shows new step added
Workflow syntax is valid (GitHub Actions validates YAML)
Step runs after build and after existing PikaCSS validation
Artifact upload configured for failure cases
  </verify>
  <done>
CI pipeline includes API verification step that blocks PR merges on documentation mismatches, with reports uploaded as artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate and document baseline</name>
  <files>
    API-VERIFICATION-BASELINE.md
  </files>
  <action>
Run verification on current codebase and document baseline state:

1. Ensure all packages are built: pnpm build

2. Run API verification: bash scripts/verify-api-docs.sh (will fail, that's expected)

3. Copy generated markdown report to baseline document:
   - cp api-verification-report.md API-VERIFICATION-BASELINE.md
   - Add header section explaining baseline purpose:
     ```markdown
     # API Verification Baseline
     
     **Generated:** [date]
     **Phase:** 3 - API Verification System
     **Purpose:** Document current state of API documentation accuracy
     
     This baseline captures all API mismatches, undocumented APIs, and contradictions
     found in the documentation as of Phase 3 completion. Future phases (4-6) will
     address these issues systematically.
     
     **Note:** This is the INITIAL baseline. Zero tolerance policy takes effect after
     Phase 7 completion. Until then, this baseline serves as improvement tracker.
     
     ---
     
     [Original report content below]
     ```

4. Add baseline to git:
   - git add API-VERIFICATION-BASELINE.md
   - Document in commit message that this is initial baseline

5. Update .gitignore to exclude generated reports (not baseline):
   - Add: /api-verification-report.json
   - Add: /api-verification-report.md
   - Keep: API-VERIFICATION-BASELINE.md (tracked)

6. Add note in baseline about limitations:
   - Plugin module augmentation requires manual review (per CONTEXT.md)
   - List packages verified: all 8 @pikacss/* packages

7. Per CONTEXT.md: "Mentioned anywhere = documented" - baseline shows which APIs are completely undocumented
  </action>
  <verify>
ls API-VERIFICATION-BASELINE.md exists
File contains baseline header and full report
File includes package coverage breakdown
grep -q "@pikacss/core" API-VERIFICATION-BASELINE.md (contains package names)
git status shows baseline is tracked, reports are ignored
  </verify>
  <done>
Baseline report documents current API documentation accuracy, providing measurable starting point for Phases 4-6 correction work.
  </done>
</task>

</tasks>

<verification>
Overall validation checks:

1. Script exists and is executable: `ls -la scripts/verify-api-docs.sh`
2. Script runs: `bash scripts/verify-api-docs.sh` (exits with appropriate code)
3. Reports generated: `ls api-verification-report.*`
4. CI workflow updated: `git diff .github/workflows/docs-validation.yml`
5. Baseline exists: `ls API-VERIFICATION-BASELINE.md`
6. Baseline is tracked: `git status API-VERIFICATION-BASELINE.md`
7. Generated reports ignored: Check .gitignore contains report paths
</verification>

<success_criteria>
- ✓ Executable CLI script runs verification and generates dual reports
- ✓ Script exits with non-zero code when mismatches found (CI-friendly)
- ✓ CI workflow includes API verification step after build
- ✓ API verification blocks PR merges when mismatches detected
- ✓ Reports uploaded as artifacts on CI failure
- ✓ Baseline report documents current API documentation state
- ✓ Baseline shows per-package coverage and undocumented APIs
- ✓ Generated reports (.json, .md) are gitignored
- ✓ Baseline document (BASELINE.md) is tracked in git
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-verification-system/03-04-SUMMARY.md`
</output>
