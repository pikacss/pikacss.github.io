---
phase: 05-integration-and-framework-layers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/unplugin/README.md
  - docs/advanced/api-reference.md
autonomous: true

must_haves:
  truths:
    - "packages/unplugin/README.md accurately describes unplugin API"
    - "PluginOptions interface documented with correct properties"
    - "All 7 bundler entry points (vite, webpack, rspack, esbuild, farm, rolldown, rollup) documented"
    - "docs/advanced/api-reference.md contains @pikacss/unplugin-pikacss section"
  artifacts:
    - path: "packages/unplugin/README.md"
      provides: "Complete @pikacss/unplugin-pikacss API documentation"
      min_lines: 100
      exports: ["default plugin factory", "PluginOptions", "ResolvedPluginOptions"]
    - path: "docs/advanced/api-reference.md"
      provides: "@pikacss/unplugin-pikacss API reference section"
      sections: ["@pikacss/unplugin-pikacss"]
  key_links:
    - from: "packages/unplugin/README.md"
      to: "packages/unplugin/src/index.ts"
      via: "unpluginFactory export"
      pattern: "unpluginFactory.*UnpluginFactory"
    - from: "packages/unplugin/README.md"
      to: "packages/unplugin/src/types.ts"
      via: "PluginOptions interface"
      pattern: "export interface PluginOptions"
---

<objective>
Correct @pikacss/unplugin-pikacss documentation to match actual universal plugin implementation.

Purpose: Unplugin layer provides the bundler-agnostic plugin interface that all framework integrations depend on. Accurate documentation is critical for multi-bundler support.

Output: packages/unplugin/README.md and docs/advanced/api-reference.md with complete, accurate API documentation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-and-framework-layers/05-RESEARCH.md

# Unplugin package source for verification
@packages/unplugin/src/index.ts
@packages/unplugin/src/types.ts
@packages/unplugin/package.json

# Phase 4 and 5-01 summaries
@.planning/phases/04-core-package-correction-pikacss-core/04-02-SUMMARY.md

# Verification infrastructure
@packages/api-verifier/src/extractor.ts
@scripts/verify-api-docs.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and correct packages/unplugin/README.md</name>
  <files>packages/unplugin/README.md</files>
  <action>
1. **Extract actual @pikacss/unplugin-pikacss exports:**
   - Read `packages/unplugin/src/index.ts` to identify public exports
   - Read `packages/unplugin/src/types.ts` for PluginOptions and ResolvedPluginOptions interfaces
   - Verify all 7 bundler-specific entry points exist:
     - `@pikacss/unplugin-pikacss/vite`
     - `@pikacss/unplugin-pikacss/webpack`
     - `@pikacss/unplugin-pikacss/rspack`
     - `@pikacss/unplugin-pikacss/esbuild`
     - `@pikacss/unplugin-pikacss/rollup`
     - `@pikacss/unplugin-pikacss/farm`
     - `@pikacss/unplugin-pikacss/rolldown`
   - Check package.json exports field for entry point confirmation

2. **Verify PluginOptions interface accuracy:**
   - scan?: { include?: string | string[], exclude?: string | string[] }
   - config?: EngineConfig | string
   - autoCreateConfig?: boolean
   - fnName?: string (default: 'pika')
   - transformedFormat?: 'string' | 'array' | 'inline' (default: 'string')
   - tsCodegen?: boolean | string (default: true → 'pika.gen.ts')
   - cssCodegen?: true | string (default: true → 'pika.gen.css')
   - currentPackageName?: string (internal option)

3. **Fix inaccuracies:**
   - Correct option property types and defaults
   - Update bundler-specific usage examples if wrong
   - Remove hallucinated options not in actual interface
   - Add missing options present in implementation
   - Ensure re-export note for `@pikacss/integration` is accurate

4. **Verify bundler-specific patterns:**
   - Vite: `import pikacss from '@pikacss/unplugin-pikacss/vite'` → `plugins: [pikacss(options)]`
   - Webpack: `const pikacss = require('@pikacss/unplugin-pikacss/webpack')` → `plugins: [pikacss(options)]`
   - Rspack: Same pattern as Webpack
   - Esbuild: `import pikacss from '@pikacss/unplugin-pikacss/esbuild'` → `plugins: [pikacss.default(options)]`
   - Rollup: `import pikacss from '@pikacss/unplugin-pikacss/rollup'`
   - Farm/Rolldown: ESM import patterns

**What to avoid:**
- Don't document framework-specific setup (belongs in framework adapter docs)
- Don't duplicate integration layer details (reference integration package)
- Don't show bundler configs that haven't been tested in Phase 2 multi-bundler tests
  </action>
  <verify>
1. Run API verifier: `bash scripts/verify-api-docs.sh` - check for mismatches in packages/unplugin/README.md
2. Verify PluginOptions: `grep -A30 "interface PluginOptions" packages/unplugin/src/types.ts`
3. Check package.json exports: `cat packages/unplugin/package.json | grep -A20 '"exports"'`
4. ESLint validation: `pnpm lint packages/unplugin/README.md`
  </verify>
  <done>
- packages/unplugin/README.md documents all exported APIs with correct signatures
- PluginOptions interface matches actual type definition
- All 7 bundler entry points documented with correct usage patterns
- No hallucinated options remain in documentation
- API verifier reports zero mismatches for unplugin package
  </done>
</task>

<task type="auto">
  <name>Task 2: Add @pikacss/unplugin-pikacss section to API reference</name>
  <files>docs/advanced/api-reference.md</files>
  <action>
1. **Locate insertion point:**
   - Find the @pikacss/core section end in docs/advanced/api-reference.md
   - Insert new section after @pikacss/core, before plugins (if plugins exist)

2. **Create @pikacss/unplugin-pikacss section:**
   - Add section header: `## @pikacss/unplugin-pikacss`
   - Document PluginOptions interface with all properties
   - Show bundler-specific import patterns for all 7 bundlers
   - Note virtual module `pika.css` and generated types `pika.gen.ts`
   - Reference integration layer for low-level API

3. **Cross-validate with packages/unplugin/README.md:**
   - Ensure option names and types match
   - Use consistent terminology
   - Verify defaults are correctly stated
   - Check that examples are syntactically correct

4. **Add ESLint disable comments if needed:**
   - Code examples in docs shouldn't trigger production rules
   - Use `<!-- eslint-disable -->` for example blocks if necessary

**What to avoid:**
- Don't duplicate @pikacss/integration details (reference it instead)
- Don't show framework-specific configs (those belong in integration guides)
- Don't add implementation details (keep it API reference level)
  </action>
  <verify>
1. Run API verifier: `bash scripts/verify-api-docs.sh` - check for contradictions in api-reference.md
2. Cross-check with README: `diff <(grep "PluginOptions" packages/unplugin/README.md) <(grep "PluginOptions" docs/advanced/api-reference.md)`
3. Validate TypeScript examples: Extract code blocks and check syntax
4. ESLint validation: `pnpm lint docs/advanced/api-reference.md`
  </verify>
  <done>
- docs/advanced/api-reference.md contains complete @pikacss/unplugin-pikacss section
- PluginOptions interface documented with all properties and defaults
- All 7 bundler entry points shown with correct import patterns
- No contradictions with packages/unplugin/README.md
- API verifier reports zero contradictions for unplugin APIs
  </done>
</task>

</tasks>

<verification>
1. **API accuracy:** Run `bash scripts/verify-api-docs.sh` and confirm zero mismatches for @pikacss/unplugin-pikacss
2. **Cross-file consistency:** packages/unplugin/README.md and docs/advanced/api-reference.md have aligned API descriptions
3. **Structural validation:** `pnpm lint` passes for both modified files
4. **Type correctness:** PluginOptions interface matches actual TypeScript definition exactly
</verification>

<success_criteria>
1. packages/unplugin/README.md documents all public exports with accurate signatures
2. PluginOptions interface matches type definition exactly with correct defaults
3. All 7 bundler entry points documented with verified usage patterns
4. docs/advanced/api-reference.md contains complete unplugin API section
5. No API mismatches or contradictions reported by verifier
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-and-framework-layers/05-02-SUMMARY.md`
</output>
