---
phase: 05-integration-and-framework-layers
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - packages/nuxt/README.md
  - docs/integrations/nuxt.md
  - packages/vite/README.md
autonomous: true

must_haves:
  truths:
    - "packages/nuxt/README.md accurately describes Nuxt module implementation"
    - "docs/integrations/nuxt.md shows correct zero-config Nuxt setup"
    - "packages/vite/README.md clearly states deprecation and migration path"
    - "Nuxt module options interface matches actual ModuleOptions type"
  artifacts:
    - path: "packages/nuxt/README.md"
      provides: "Complete @pikacss/nuxt-pikacss documentation"
      min_lines: 80
    - path: "docs/integrations/nuxt.md"
      provides: "Complete Nuxt integration guide"
      min_lines: 100
    - path: "packages/vite/README.md"
      provides: "Deprecation notice and migration guide"
      min_lines: 30
  key_links:
    - from: "packages/nuxt/README.md"
      to: "packages/nuxt/src/index.ts"
      via: "Nuxt module export"
      pattern: "defineNuxtModule"
---

<objective>
Correct Nuxt framework adapter documentation and deprecate @pikacss/vite-plugin-pikacss package.

Purpose: Nuxt is a major framework integration requiring zero-config experience. Vite plugin package needs clear deprecation to guide users to unplugin.

Output: Accurate Nuxt documentation and clear migration guide for deprecated Vite package.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-and-framework-layers/05-RESEARCH.md

# Prior plan summaries
# 05-02: PluginOptions verified
# 05-03: Vite integration guide corrected

# Source verification
@packages/nuxt/src/index.ts
@packages/vite/src/index.ts
@packages/nuxt/package.json
@packages/vite/package.json

# Verification infrastructure
@scripts/verify-api-docs.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and correct Nuxt module documentation</name>
  <files>packages/nuxt/README.md, docs/integrations/nuxt.md</files>
  <action>
1. **Extract actual Nuxt module implementation:**
   - Read `packages/nuxt/src/index.ts` to understand defineNuxtModule usage
   - Identify ModuleOptions type (extends PluginOptions minus currentPackageName)
   - Verify automatic behaviors:
     - Auto-injects pika.css via addPluginTemplate
     - Auto-configures scan patterns for Vue files
     - Passes options to unplugin/vite internally

2. **Verify packages/nuxt/README.md:**
   - Installation: `npm install -D @pikacss/nuxt-pikacss`
   - Usage in nuxt.config.ts: `modules: ['@pikacss/nuxt-pikacss']`
   - Options: Same as PluginOptions (scan, config, fnName, etc.) minus currentPackageName
   - Note automatic features (CSS injection, Vue scanning)
   - Verify default scan patterns: `['**/*.vue', '**/*.tsx', '**/*.jsx']`

3. **Verify docs/integrations/nuxt.md:**
   - Zero-config setup process
   - Optional configuration via `pikacss: {}` in nuxt.config.ts
   - Note that pika.css import is NOT needed (auto-injected)
   - pika() function globally available (no import needed)
   - TypeScript reference setup for IDE support

4. **Fix inaccuracies:**
   - Correct any wrong option types (reference 05-02)
   - Update automatic behaviors to match implementation
   - Remove manual steps that are automated (like CSS import)
   - Ensure Nuxt 3 compatibility noted

**What to avoid:**
- Don't show manual pika.css import (Nuxt auto-injects it)
- Don't duplicate unplugin options docs (reference them)
- Don't show Nuxt 2 examples (module targets Nuxt 3)
  </action>
  <verify>
1. Check module definition: `grep "defineNuxtModule" packages/nuxt/src/index.ts`
2. Verify auto-injection: `grep "addPluginTemplate" packages/nuxt/src/index.ts`
3. Validate options: Compare ModuleOptions with PluginOptions from 05-02
4. ESLint validation: `pnpm lint packages/nuxt/README.md docs/integrations/nuxt.md`
  </verify>
  <done>
- packages/nuxt/README.md documents all module features accurately
- docs/integrations/nuxt.md shows correct zero-config setup
- Automatic behaviors (CSS injection, Vue scanning) documented
- Options reference PluginOptions from 05-02
- No manual steps shown that are automated
  </done>
</task>

<task type="auto">
  <name>Task 2: Add deprecation notice to @pikacss/vite-plugin-pikacss</name>
  <files>packages/vite/README.md</files>
  <action>
1. **Verify current state:**
   - Read `packages/vite/src/index.ts` to confirm it's a wrapper
   - Check that it re-exports from @pikacss/unplugin-pikacss/vite
   - Verify package.json indicates deprecation (if present)

2. **Add prominent deprecation notice:**
   - Add at top of README: "⚠️ DEPRECATED - Use @pikacss/unplugin-pikacss/vite instead"
   - Explain deprecation reason: Package consolidated into unplugin for multi-bundler support
   - Show migration path with before/after code

3. **Migration guide:**
   - Before (deprecated):
     ```typescript
     import pikacss from '@pikacss/vite-plugin-pikacss'
     ```
   - After (current):
     ```typescript
     import pikacss from '@pikacss/unplugin-pikacss/vite'
     ```
   - Note: Options remain identical (no breaking changes in API)
   - Mention deprecation timeline (if package will be removed)

4. **Keep functional documentation minimal:**
   - Since package still works (wrapper), show basic usage
   - Link to current documentation: docs/integrations/vite.md
   - Note: Users should migrate but package maintains compatibility

**What to avoid:**
- Don't remove package (may break existing projects)
- Don't show extensive examples (encourage migration instead)
- Don't duplicate Vite integration docs (link to current guide)
  </action>
  <verify>
1. Check wrapper implementation: `cat packages/vite/src/index.ts`
2. Verify it re-exports unplugin: `grep "from '@pikacss/unplugin-pikacss/vite'" packages/vite/src/index.ts`
3. ESLint validation: `pnpm lint packages/vite/README.md`
4. Link validation: Ensure link to docs/integrations/vite.md is correct
  </verify>
  <done>
- packages/vite/README.md has prominent deprecation warning
- Migration guide shows exact code changes needed
- Links to current Vite integration documentation
- Package maintains backward compatibility as wrapper
- Users understand to use unplugin directly going forward
  </done>
</task>

</tasks>

<verification>
1. **API accuracy:** Run `bash scripts/verify-api-docs.sh` and confirm zero mismatches for Nuxt package
2. **Nuxt module behavior:** Verify automatic CSS injection and scanning behavior match implementation
3. **Deprecation clarity:** Vite package README clearly guides users to unplugin
4. **Structural validation:** `pnpm lint` passes for all three files
</verification>

<success_criteria>
1. Nuxt module documentation accurately describes zero-config setup
2. Automatic features (CSS injection, Vue scanning) clearly documented
3. Nuxt options reference verified PluginOptions from 05-02
4. Deprecated Vite package has clear migration guide
5. No API mismatches or contradictions in Nuxt documentation
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-and-framework-layers/05-05-SUMMARY.md`
</output>
