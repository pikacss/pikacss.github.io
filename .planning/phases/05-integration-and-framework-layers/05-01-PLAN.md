---
phase: 05-integration-and-framework-layers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/integration/README.md
  - AGENTS.md
autonomous: true

must_haves:
  truths:
    - "packages/integration/README.md accurately describes exported APIs"
    - "@pikacss/integration createCtx() function documented with correct signature"
    - "IntegrationContext interface matches actual type definition"
    - "AGENTS.md integration layer description reflects actual implementation"
  artifacts:
    - path: "packages/integration/README.md"
      provides: "Complete @pikacss/integration API documentation"
      min_lines: 50
      exports: ["createCtx", "IntegrationContext", "IntegrationContextOptions"]
    - path: "AGENTS.md"
      provides: "Accurate integration layer architecture"
      sections: ["@pikacss/integration description"]
  key_links:
    - from: "packages/integration/README.md"
      to: "packages/integration/src/ctx.ts"
      via: "createCtx export"
      pattern: "export.*createCtx"
    - from: "packages/integration/README.md"
      to: "packages/integration/src/types.ts"
      via: "type definitions"
      pattern: "export interface IntegrationContext"
---

<objective>
Correct @pikacss/integration documentation to match actual low-level integration API.

Purpose: Establish accurate foundation for unplugin and framework adapter documentation. Integration layer is the bridge between core engine and build tools.

Output: packages/integration/README.md with complete, accurate API documentation and updated AGENTS.md section.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-and-framework-layers/05-RESEARCH.md

# Integration package source for verification
@packages/integration/src/index.ts
@packages/integration/src/ctx.ts
@packages/integration/src/types.ts
@packages/integration/package.json

# Phase 4 summaries for @pikacss/core API references
@.planning/phases/04-core-package-correction-pikacss-core/04-02-SUMMARY.md

# Verification infrastructure
@packages/api-verifier/src/extractor.ts
@scripts/verify-api-docs.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and correct packages/integration/README.md</name>
  <files>packages/integration/README.md</files>
  <action>
1. **Extract actual @pikacss/integration exports:**
   - Read `packages/integration/src/index.ts` to identify all public exports
   - Read `packages/integration/src/ctx.ts` for createCtx() implementation
   - Read `packages/integration/src/types.ts` for IntegrationContext and IntegrationContextOptions interfaces
   - Use API verifier: `node packages/api-verifier/dist/cli.mjs --package integration` (if needed)

2. **Verify README.md accuracy:**
   - Check that createCtx() signature matches implementation (async function returning IntegrationContext)
   - Verify IntegrationContextOptions properties match type definition:
     - cwd: string
     - currentPackageName: string
     - scan: { include: string[], exclude: string[] }
     - configOrPath: EngineConfig | string | Nullish
     - fnName: string
     - transformedFormat: 'string' | 'array' | 'inline'
     - tsCodegen: false | string
     - cssCodegen: string
     - autoCreateConfig: boolean
   - Verify IntegrationContext interface documented correctly (all properties and methods)
   - Check for hallucinated APIs not present in source

3. **Fix inaccuracies:**
   - Update createCtx() signature if incorrect
   - Correct any wrong property types or names
   - Add missing exports or properties
   - Remove any APIs that don't exist in actual implementation
   - Ensure examples show correct usage patterns

4. **Verify re-exports:**
   - Confirm that `@pikacss/core` re-exports are mentioned
   - Note that integration layer extends core with build-time features

**What to avoid:**
- Don't document internal utilities (prefixed with `_` or not exported)
- Don't describe unplugin-specific features (those belong in unplugin docs)
- Don't modify code to match documentation (doc corrects to match code)
  </action>
  <verify>
1. Run API verifier: `bash scripts/verify-api-docs.sh` - check for mismatches in packages/integration/README.md
2. Verify exports match: `diff <(grep "export" packages/integration/src/index.ts) <(grep "^##.*API" packages/integration/README.md)`
3. Check IntegrationContext properties: `grep -A50 "interface IntegrationContext" packages/integration/src/types.ts`
4. ESLint validation: `pnpm lint packages/integration/README.md`
  </verify>
  <done>
- packages/integration/README.md documents all exported APIs with correct signatures
- createCtx() function signature matches implementation
- IntegrationContext and IntegrationContextOptions interfaces accurately documented
- No hallucinated APIs remain in documentation
- API verifier reports zero mismatches for integration package
  </done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md integration layer description</name>
  <files>AGENTS.md</files>
  <action>
1. **Locate integration layer section:**
   - Find "**`@pikacss/integration`**" section in AGENTS.md (around line 70-90)
   - Identify layered architecture diagram (around line 50-80)

2. **Verify accuracy against implementation:**
   - Check that "Build-time tools" description matches actual role
   - Verify dependency list: should only depend on @pikacss/core
   - Confirm provides low-level API (createCtx, IntegrationContext)
   - Validate transformation pipeline description

3. **Fix inaccuracies:**
   - Update responsibilities if description is vague or incorrect
   - Correct any wrong dependency relationships
   - Fix any contradictions with packages/integration/README.md
   - Ensure consistent terminology (e.g., "context" vs "integration context")

4. **Cross-validate with Phase 4:**
   - Ensure integration layer description doesn't contradict corrected @pikacss/core section
   - Verify dependency chain: core → integration → unplugin → framework adapters

**What to avoid:**
- Don't add implementation details (keep it architectural overview)
- Don't duplicate API documentation from README (AGENTS.md is high-level)
- Don't modify layered architecture diagram unless factually wrong
  </action>
  <verify>
1. Check consistency: `diff <(grep -A10 "@pikacss/integration" AGENTS.md) <(grep -A10 "# @pikacss/integration" packages/integration/README.md)`
2. Verify no contradictions: `bash scripts/verify-api-docs.sh` includes AGENTS.md
3. Cross-reference Phase 4: Ensure @pikacss/core description alignment
4. ESLint validation: `pnpm lint AGENTS.md`
  </verify>
  <done>
- AGENTS.md accurately describes @pikacss/integration responsibilities
- Integration layer positioned correctly in dependency chain
- No contradictions with packages/integration/README.md
- Terminology consistent across documentation
- API verifier reports zero contradictions for integration layer
  </done>
</task>

</tasks>

<verification>
1. **API accuracy:** Run `bash scripts/verify-api-docs.sh` and confirm zero mismatches for @pikacss/integration
2. **Cross-file consistency:** AGENTS.md and packages/integration/README.md have aligned descriptions
3. **Structural validation:** `pnpm lint` passes for both modified files
4. **Type correctness:** All documented interfaces match actual TypeScript definitions
</verification>

<success_criteria>
1. packages/integration/README.md documents all public exports with accurate signatures
2. IntegrationContext and IntegrationContextOptions interfaces match type definitions exactly
3. AGENTS.md integration layer section reflects actual implementation
4. No API mismatches reported by verifier for integration package
5. All verification checks pass without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-and-framework-layers/05-01-SUMMARY.md`
</output>
