---
phase: 05-integration-and-framework-layers
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - docs/integrations/vite.md
  - docs/integrations/webpack.md
  - docs/integrations/rspack.md
  - docs/integrations/esbuild.md
autonomous: true

must_haves:
  truths:
    - "All Vite integration examples use correct @pikacss/unplugin-pikacss/vite import"
    - "Webpack examples show correct plugin instantiation pattern"
    - "Rspack examples follow Webpack-compatible pattern"
    - "Esbuild examples use .default() for plugin"
    - "All examples reference verified PluginOptions from 05-02"
  artifacts:
    - path: "docs/integrations/vite.md"
      provides: "Complete Vite integration guide"
      min_lines: 100
    - path: "docs/integrations/webpack.md"
      provides: "Complete Webpack integration guide"
      min_lines: 80
    - path: "docs/integrations/rspack.md"
      provides: "Complete Rspack integration guide"
      min_lines: 60
    - path: "docs/integrations/esbuild.md"
      provides: "Complete Esbuild integration guide"
      min_lines: 60
  key_links:
    - from: "docs/integrations/vite.md"
      to: "packages/unplugin/src/vite.ts"
      via: "Vite plugin export"
      pattern: "export default.*vite"
---

<objective>
Correct bundler integration guides (Vite, Webpack, Rspack, Esbuild) to match verified unplugin API.

Purpose: These are primary user-facing integration guides. Must be accurate and executable after Phase 5-02 established correct PluginOptions.

Output: Four integration guide files with accurate, tested examples.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-and-framework-layers/05-RESEARCH.md

# Plan 05-02 summary (unplugin API)
# Must be completed first to reference correct PluginOptions

# Source verification
@packages/unplugin/src/vite.ts
@packages/unplugin/src/webpack.ts
@packages/unplugin/src/rspack.ts
@packages/unplugin/src/esbuild.ts

# Multi-bundler test fixtures from Phase 2
@.eslint/tests/integration/bundlers.test.ts

# Verification infrastructure
@scripts/verify-api-docs.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct Vite integration guide</name>
  <files>docs/integrations/vite.md</files>
  <action>
1. **Verify import pattern:**
   - Must use: `import pikacss from '@pikacss/unplugin-pikacss/vite'`
   - NOT: `import pikacss from '@pikacss/vite-plugin-pikacss'` (deprecated)
   - Verify against packages/unplugin/src/vite.ts export

2. **Verify plugin usage:**
   - Plugin instantiation: `plugins: [pikacss(options)]`
   - Verify PluginOptions against 05-02 corrections
   - Check default values match implementation

3. **Verify setup steps:**
   - Installation command correct
   - vite.config.ts example accurate
   - Virtual module import shown: `import 'pika.css'`
   - TypeScript reference shown: `/// <reference path="./src/pika.gen.ts" />`
   - pika.config.ts example valid

4. **Fix inaccuracies:**
   - Update deprecated imports
   - Correct option names/types
   - Fix any hallucinated features
   - Ensure examples are build-time compliant (from Phase 2)

**What to avoid:**
- Don't show framework-specific setup (React/Vue plugins separate from PikaCSS)
- Don't duplicate PluginOptions docs (reference API reference)
- Don't include untested examples
  </action>
  <verify>
1. Cross-check import: `grep "export default" packages/unplugin/src/vite.ts`
2. Validate options: Compare with packages/unplugin/README.md from 05-02
3. Check build-time compliance: Extract examples and verify against Phase 2 rules
4. ESLint validation: `pnpm lint docs/integrations/vite.md`
  </verify>
  <done>
- docs/integrations/vite.md uses correct unplugin import
- All examples use verified PluginOptions
- Setup steps are complete and accurate
- Virtual module and TypeScript setup documented
- No deprecated patterns remain
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct Webpack, Rspack, Esbuild integration guides</name>
  <files>docs/integrations/webpack.md, docs/integrations/rspack.md, docs/integrations/esbuild.md</files>
  <action>
1. **Webpack (docs/integrations/webpack.md):**
   - Import: `const pikacss = require('@pikacss/unplugin-pikacss/webpack')`
   - Usage: `plugins: [pikacss(options)]`
   - Note: CJS format typical for Webpack configs
   - Verify against packages/unplugin/src/webpack.ts

2. **Rspack (docs/integrations/rspack.md):**
   - Import: `const pikacss = require('@pikacss/unplugin-pikacss/rspack')`
   - Usage: Same as Webpack (Webpack-compatible API)
   - Note: Rspack-specific optimizations if documented
   - Verify against packages/unplugin/src/rspack.ts

3. **Esbuild (docs/integrations/esbuild.md):**
   - Import: `import pikacss from '@pikacss/unplugin-pikacss/esbuild'`
   - Usage: `plugins: [pikacss.default(options)]` (note .default())
   - Note: Esbuild requires .default() for ESM imports
   - Verify against packages/unplugin/src/esbuild.ts

4. **Common fixes for all three:**
   - Update PluginOptions to match 05-02 corrections
   - Ensure virtual module import shown
   - Verify TypeScript setup instructions
   - Check that pika.config.ts example is consistent across guides

5. **Cross-validate:**
   - All three guides should have similar structure
   - Option examples should be identical (only bundler config syntax differs)
   - Troubleshooting sections should be consistent

**What to avoid:**
- Don't show Vite-specific features in these guides
- Don't duplicate full option docs (reference API reference)
- Don't mix bundler-specific config syntax
  </action>
  <verify>
1. Check exports: 
   - `grep "export default" packages/unplugin/src/webpack.ts`
   - `grep "export default" packages/unplugin/src/rspack.ts`
   - `grep "export default" packages/unplugin/src/esbuild.ts`
2. Validate option consistency: Compare PluginOptions usage across all 4 files
3. ESLint validation: `pnpm lint docs/integrations/{webpack,rspack,esbuild}.md`
4. Build-time compliance: Verify examples pass Phase 2 rules
  </verify>
  <done>
- All three guides use correct bundler-specific imports
- Webpack and Rspack follow Webpack-compatible patterns
- Esbuild correctly uses .default() pattern
- PluginOptions match 05-02 corrections in all guides
- Virtual module and TypeScript setup consistent across guides
  </done>
</task>

</tasks>

<verification>
1. **API accuracy:** Run `bash scripts/verify-api-docs.sh` and confirm zero mismatches for integration guides
2. **Cross-file consistency:** All four guides use identical PluginOptions examples
3. **Structural validation:** `pnpm lint` passes for all four files
4. **Build-time compliance:** All examples pass Phase 2 pika-build-time rule
</verification>

<success_criteria>
1. Vite guide uses correct unplugin import, not deprecated package
2. Webpack and Rspack guides follow Webpack-compatible patterns
3. Esbuild guide correctly shows .default() usage
4. All guides reference verified PluginOptions from Phase 5-02
5. No deprecated patterns or hallucinated features remain
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-and-framework-layers/05-03-SUMMARY.md`
</output>
