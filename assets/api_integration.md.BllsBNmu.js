import{_ as t,o as d,i as o,ad as a}from"./chunks/framework.Cq4hg8jX.js";const g=JSON.parse('{"title":"Integration API reference","description":"Generated API reference for @pikacss/integration from exported surface and JSDoc.","frontmatter":{"title":"Integration API reference","description":"Generated API reference for @pikacss/integration from exported surface and JSDoc.","outline":[2,3],"relatedPackages":["@pikacss/integration"],"relatedSources":["packages/integration/src/compiler/analyze.ts","packages/integration/src/compiler/errors.ts","packages/integration/src/compiler/evaluate.ts","packages/integration/src/compiler/parse.ts","packages/integration/src/ctx.ts","packages/integration/src/fnConfig.ts","packages/integration/src/index.ts","packages/integration/src/log.ts","packages/integration/src/moduleId.ts","packages/integration/src/processors/js.ts","packages/integration/src/processors/registry.ts","packages/integration/src/processors/types.ts","packages/integration/src/types.ts"],"category":"api","order":30},"headers":[],"relativePath":"api/integration.md","filePath":"api/integration.md"}'),r={name:"api/integration.md"};function i(n,e,s,c,l,h){return d(),o("div",null,[...e[0]||(e[0]=[a(`<div style="display:none;" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /api/integration.md for this page in Markdown format</div><h1 id="integration-api-reference" tabindex="-1">Integration API reference <a class="header-anchor" href="#integration-api-reference" aria-label="Permalink to “Integration API reference”">​</a></h1><details><summary>Source information</summary><ul><li>Package: <code>@pikacss/integration</code></li><li>Generated from the exported surface and JSDoc in <code>packages/integration/src/index.ts</code>.</li><li>Source files: <code>packages/integration/src/compiler/analyze.ts</code>, <code>packages/integration/src/compiler/errors.ts</code>, <code>packages/integration/src/compiler/evaluate.ts</code>, <code>packages/integration/src/compiler/parse.ts</code>, <code>packages/integration/src/ctx.ts</code>, <code>packages/integration/src/fnConfig.ts</code>, <code>packages/integration/src/index.ts</code>, <code>packages/integration/src/log.ts</code>, <code>packages/integration/src/moduleId.ts</code>, <code>packages/integration/src/processors/js.ts</code>, <code>packages/integration/src/processors/registry.ts</code>, <code>packages/integration/src/processors/types.ts</code>, <code>packages/integration/src/types.ts</code></li></ul></details><h2 id="package-summary" tabindex="-1">Package summary <a class="header-anchor" href="#package-summary" aria-label="Permalink to “Package summary”">​</a></h2><p>Build-tool integration context Re-exports the public surface of <a href="/api/core.html"><code>@pikacss/core</code></a>.</p><p>Use <a href="/integrations/unplugin.html">Unplugin integration</a> when you need conceptual usage guidance instead of exact symbol lookup.</p><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to “Functions”">​</a></h2><h3 id="function-analyzejs-code-id-dialect-fnconfig-options" tabindex="-1">analyzeJs(code, id, dialect, fnConfig, options?) <a class="header-anchor" href="#function-analyzejs-code-id-dialect-fnconfig-options" aria-label="Permalink to “analyzeJs(code, id, dialect, fnConfig, options?)”">​</a></h3><p>Analyzes a JavaScript/TypeScript source chunk: parse, collect macro calls, and statically evaluate each call&#39;s arguments.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>code</code></td><td><code>string</code></td><td>The source chunk.</td></tr><tr><td><code>id</code></td><td><code>string</code></td><td>Normalized absolute path of the module (for diagnostics).</td></tr><tr><td><code>dialect</code></td><td><code>JsDialect</code></td><td>The JsDialect deciding the parser plugin set.</td></tr><tr><td><code>fnConfig</code></td><td><code>FnConfig</code></td><td>The variant config derived from the base function name.</td></tr><tr><td><code>options?</code></td><td><code>AnalyzeJsOptions</code></td><td>Optional AnalyzeJsOptions.</td></tr></tbody></table><p><strong>Returns:</strong> <code>MacroCall[]</code> - Macro calls sorted by start offset. Offsets are absolute into the surrounding file when <code>options.offsets</code> is set.</p><br><br><h3 id="function-createctx-options" tabindex="-1">createCtx(options) <a class="header-anchor" href="#function-createctx-options" aria-label="Permalink to “createCtx(options)”">​</a></h3><p>Creates an <code>IntegrationContext</code> that wires together config loading, engine initialization, source file transformation, and codegen output.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>options</code></td><td><code>IntegrationContextOptions</code></td><td>The integration configuration including paths, function name, scan globs, and codegen settings.</td></tr></tbody></table><p><strong>Returns:</strong> <code>IntegrationContext</code> - A fully constructed <code>IntegrationContext</code>. Call <code>setup()</code> on the returned context before using transforms.</p><p>The context uses reactive signals internally so that computed paths (CSS and TS codegen file paths) automatically update when <code>cwd</code> changes. The <code>setup()</code> method must be called before any transform or codegen operations - transform calls automatically await the pending setup promise.</p><br><br><h3 id="function-createdefaultprocessorregistry" tabindex="-1">createDefaultProcessorRegistry() <a class="header-anchor" href="#function-createdefaultprocessorregistry" aria-label="Permalink to “createDefaultProcessorRegistry()”">​</a></h3><p>Creates the default processor registry: the JS/TS processor (static import — it is the hot path) and the Vue SFC processor (lazy — <code>@vue/compiler-sfc</code> never loads in non-Vue projects).</p><p><strong>Returns:</strong> <code>ProcessorRegistry</code> - The default ProcessorRegistry.</p><br><br><h3 id="function-createfnconfig-fnname" tabindex="-1">createFnConfig(fnName) <a class="header-anchor" href="#function-createfnconfig-fnname" aria-label="Permalink to “createFnConfig(fnName)”">​</a></h3><p>Builds the structured variant config for all <code>pika()</code> call forms derived from the given base name.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>fnName</code></td><td><code>string</code></td><td>The base function name (e.g. <code>&#39;pika&#39;</code>). The preview name (<code>p</code> suffix) and <code>.str</code>/<code>.arr</code> members are derived from it.</td></tr></tbody></table><p><strong>Returns:</strong> <code>FnConfig</code> - An immutable FnConfig describing all six variants.</p><p><strong>Remarks:</strong></p><p>Keep variant derivation in sync with <code>buildFnNamePatterns</code> in <code>@pikacss/eslint-config</code> (<code>packages/eslint-config/src/utils/fn-names.ts</code>), which re-derives the same dot-form variants without a runtime dependency on this package. The consistency test in its <code>fn-names.test.ts</code> guards the agreement.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> config</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createFnConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pika&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config.roots.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">has</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pikap&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config.variants.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pika.str&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)?.kind </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;forceString&#39;</span></span></code></pre></div><br><br><h3 id="function-createprocessorregistry" tabindex="-1">createProcessorRegistry() <a class="header-anchor" href="#function-createprocessorregistry" aria-label="Permalink to “createProcessorRegistry()”">​</a></h3><p>Creates an empty processor registry.</p><p><strong>Returns:</strong> <code>ProcessorRegistry</code> - A ProcessorRegistry with case-insensitive extension keys and memoized lazy loading.</p><br><br><h3 id="function-dialectforextension-ext" tabindex="-1">dialectForExtension(ext) <a class="header-anchor" href="#function-dialectforextension-ext" aria-label="Permalink to “dialectForExtension(ext)”">​</a></h3><p>Maps a file extension to the JsDialect it is parsed as.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>ext</code></td><td><code>string</code></td><td>Lowercase extension without the leading dot.</td></tr></tbody></table><p><strong>Returns:</strong> <code>JsDialect</code> - The dialect; unknown extensions fall back to <code>&#39;js&#39;</code>.</p><br><br><h3 id="function-evaluatestatic-node-ctx" tabindex="-1">evaluateStatic(node, ctx) <a class="header-anchor" href="#function-evaluatestatic-node-ctx" aria-label="Permalink to “evaluateStatic(node, ctx)”">​</a></h3><p>Statically evaluates a macro-call argument AST node to a plain value.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>node</code></td><td><code>t.Node</code></td><td>The argument expression node.</td></tr><tr><td><code>ctx</code></td><td><code>EvaluateContext</code></td><td>The EvaluateContext carrying the module id and scope lookup.</td></tr></tbody></table><p><strong>Returns:</strong> <code>unknown</code> - The evaluated plain value (JSON-serializable by construction, plus <code>undefined</code>).</p><p><strong>Remarks:</strong></p><p>Replaces the legacy <code>new Function()</code> evaluation of argument source text. Supported: literals, <code>undefined</code>/<code>NaN</code>/<code>Infinity</code> (when unshadowed), unary <code>- + ! void</code>, static template literals, object/array expressions (including static computed keys, spreads, and holes), conditional and logical short-circuits, and binary <code>+ - * / === !==</code> on static operands.</p><br><br><h3 id="function-nodeloc-node" tabindex="-1">nodeLoc(node) <a class="header-anchor" href="#function-nodeloc-node" aria-label="Permalink to “nodeLoc(node)”">​</a></h3><p>Extracts a TransformErrorLoc from an AST node&#39;s source location.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>node</code></td><td><code>{ loc?: { start: { line: number, column: number } } | null }</code></td><td>Any node carrying an optional Babel-style <code>loc</code>.</td></tr></tbody></table><p><strong>Returns:</strong> <code>TransformErrorLoc \\| null</code> - The start position, or <code>null</code> when the node has no location info.</p><br><br><h3 id="function-parsejs-code-dialect-offsets" tabindex="-1">parseJs(code, dialect, offsets?) <a class="header-anchor" href="#function-parsejs-code-dialect-offsets" aria-label="Permalink to “parseJs(code, dialect, offsets?)”">​</a></h3><p>Parses a JavaScript/TypeScript source file into a Babel AST.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>code</code></td><td><code>string</code></td><td>The source chunk to parse.</td></tr><tr><td><code>dialect</code></td><td><code>JsDialect</code></td><td>The JsDialect deciding the parser plugin set.</td></tr><tr><td><code>offsets?</code></td><td><code>ParseOffsets</code></td><td>Optional ParseOffsets making emitted positions absolute into a surrounding file.</td></tr></tbody></table><p><strong>Returns:</strong> <code>t.File</code> - The parsed <code>File</code> node.</p><br><br><h3 id="function-parsejsexpression-code-dialect-offsets" tabindex="-1">parseJsExpression(code, dialect, offsets?) <a class="header-anchor" href="#function-parsejsexpression-code-dialect-offsets" aria-label="Permalink to “parseJsExpression(code, dialect, offsets?)”">​</a></h3><p>Parses a bare JavaScript/TypeScript expression (e.g. a Vue template expression) into a Babel AST node.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>code</code></td><td><code>string</code></td><td>The expression source.</td></tr><tr><td><code>dialect</code></td><td><code>JsDialect</code></td><td>The JsDialect deciding the parser plugin set.</td></tr><tr><td><code>offsets?</code></td><td><code>ParseOffsets</code></td><td>Optional ParseOffsets making emitted positions absolute into a surrounding file.</td></tr></tbody></table><p><strong>Returns:</strong> <code>t.Expression</code> - The parsed expression node.</p><br><br><h3 id="function-parsemoduleid-id-cwd" tabindex="-1">parseModuleId(id, cwd) <a class="header-anchor" href="#function-parsemoduleid-id-cwd" aria-label="Permalink to “parseModuleId(id, cwd)”">​</a></h3><p>Parses a bundler module id into its canonical identity.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>id</code></td><td><code>string</code></td><td>A module id: absolute or <code>cwd</code>-relative file path, optionally carrying <code>?query</code> and/or <code>#hash</code> suffixes.</td></tr><tr><td><code>cwd</code></td><td><code>string</code></td><td>The base directory used to resolve relative ids.</td></tr></tbody></table><p><strong>Returns:</strong> <code>ParsedModuleId</code> - The ParsedModuleId with a normalized absolute <code>file</code>, the raw <code>query</code> (hash excluded), and the lowercase <code>ext</code>.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseModuleId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;src/App.vue?vue&amp;type=script&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/repo&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// { file: &#39;/repo/src/App.vue&#39;, query: &#39;vue&amp;type=script&#39;, ext: &#39;vue&#39; }</span></span></code></pre></div><br><br><h3 id="function-resolveoutputformat-variant-transformedformat" tabindex="-1">resolveOutputFormat(variant, transformedFormat) <a class="header-anchor" href="#function-resolveoutputformat-variant-transformedformat" aria-label="Permalink to “resolveOutputFormat(variant, transformedFormat)”">​</a></h3><p>Resolves the concrete output format for a call variant under the given default format.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>variant</code></td><td><code>FnVariant</code></td><td>The matched call variant.</td></tr><tr><td><code>transformedFormat</code></td><td><code>&#39;string&#39; | &#39;array&#39;</code></td><td>The integration&#39;s configured default output format for normal calls.</td></tr></tbody></table><p><strong>Returns:</strong> <code>&#39;string&#39; \\| &#39;array&#39;</code> - <code>&#39;string&#39;</code> or <code>&#39;array&#39;</code> — the format the transformed literal must use.</p><br><br><h2 id="constants" tabindex="-1">Constants <a class="header-anchor" href="#constants" aria-label="Permalink to “Constants”">​</a></h2><h3 id="const-consolediagnostichandler" tabindex="-1">consoleDiagnosticHandler <a class="header-anchor" href="#const-consolediagnostichandler" aria-label="Permalink to “consoleDiagnosticHandler”">​</a></h3><p>Default diagnostic adapter used by official Node.js integrations.</p><br><br><h3 id="const-js-processor-extensions" tabindex="-1">JS_PROCESSOR_EXTENSIONS <a class="header-anchor" href="#const-js-processor-extensions" aria-label="Permalink to “JS_PROCESSOR_EXTENSIONS”">​</a></h3><p>File extensions handled by the built-in JS/TS processor.</p><br><br><h3 id="const-jsprocessor" tabindex="-1">jsProcessor <a class="header-anchor" href="#const-jsprocessor" aria-label="Permalink to “jsProcessor”">​</a></h3><p>The built-in JavaScript/TypeScript processor.</p><p><strong>Remarks:</strong></p><p>Emitted literals always use single quotes for JS sources (engine invariant: the transformed output convention predates the AST compiler and is pinned by regression tests).</p><br><br><h3 id="const-log" tabindex="-1">log <a class="header-anchor" href="#const-log" aria-label="Permalink to “log”">​</a></h3><p>Console-backed logger used by Node.js build-tool integrations.</p><br><br><h2 id="classes" tabindex="-1">Classes <a class="header-anchor" href="#classes" aria-label="Permalink to “Classes”">​</a></h2><h3 id="class-pikatransformerror" tabindex="-1">PikaTransformError <a class="header-anchor" href="#class-pikatransformerror" aria-label="Permalink to “PikaTransformError”">​</a></h3><p>Error thrown when a module cannot be transformed.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>id</code></td><td><code>string</code></td><td>Normalized absolute path of the failing module.</td><td>—</td></tr><tr><td><code>loc</code></td><td><code>TransformErrorLoc | null</code></td><td>One-based position of the failure inside the module, when known.</td><td>—</td></tr><tr><td><code>stage</code></td><td><code>TransformErrorStage</code></td><td>Pipeline stage that failed.</td><td>—</td></tr></tbody></table><p><strong>Remarks:</strong></p><p>Module transforms are atomic: any failure aborts the whole module without committing partial results, and this error propagates to the bundler (dev overlay / failed build). The <code>id</code> and <code>loc</code> fields follow the shape bundlers (Vite/Rollup) read to render code frames for plugin errors.</p><br><br><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to “Types”">​</a></h2><h3 id="interface-analyzedmodule" tabindex="-1">AnalyzedModule <a class="header-anchor" href="#interface-analyzedmodule" aria-label="Permalink to “AnalyzedModule”">​</a></h3><p>Result of analyzing one module: every macro call found in it.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>id</code></td><td><code>string</code></td><td>Normalized absolute path of the module.</td><td>—</td></tr><tr><td><code>code</code></td><td><code>string</code></td><td>The exact source that was analyzed.</td><td>—</td></tr><tr><td><code>calls</code></td><td><code>MacroCall[]</code></td><td>Macro calls sorted by <code>start</code> offset (deterministic within-module order).</td><td>—</td></tr></tbody></table><br><br><h3 id="interface-analyzejsoptions" tabindex="-1">AnalyzeJsOptions <a class="header-anchor" href="#interface-analyzejsoptions" aria-label="Permalink to “AnalyzeJsOptions”">​</a></h3><p>Options for analyzeJs.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>offsets?</code></td><td><code>ParseOffsets</code></td><td>Position offsets when the chunk is embedded in a surrounding file (e.g. a Vue SFC script block).</td><td>—</td></tr><tr><td><code>quote?</code></td><td><code>&#39;&quot;&#39; | &#39;\\&#39;&#39;</code></td><td>Quote character for emitted literals at the found call sites.</td><td><code>&#39;</code></td></tr><tr><td><code>parseMode?</code></td><td><code>&#39;program&#39; | &#39;expression&#39;</code></td><td>How to parse the chunk. <code>&#39;expression&#39;</code> parses a bare expression (e.g. a Vue template interpolation, where <code>{ a: 1 }</code> must be an object literal, not a block statement).</td><td><code>&#39;program&#39;</code></td></tr><tr><td><code>excludedRoots?</code></td><td><code>ReadonlySet&lt;string&gt;</code></td><td>Root identifiers shadowed by the surrounding non-JS context (e.g. Vue <code>v-for</code> aliases); calls through them are not macros.</td><td>—</td></tr></tbody></table><br><br><h3 id="interface-evaluatecontext" tabindex="-1">EvaluateContext <a class="header-anchor" href="#interface-evaluatecontext" aria-label="Permalink to “EvaluateContext”">​</a></h3><p>Context for statically evaluating a macro-call argument.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>id</code></td><td><code>string</code></td><td>Normalized absolute path of the module, used in error messages.</td><td>—</td></tr><tr><td><code>hasLocalBinding</code></td><td><code>(name: string) =&gt; boolean</code></td><td>Returns whether the given name resolves to a local binding at the call site. Global constants (<code>undefined</code>, <code>NaN</code>, <code>Infinity</code>) are only evaluable when unshadowed.</td><td>—</td></tr></tbody></table><br><br><h3 id="interface-fnconfig" tabindex="-1">FnConfig <a class="header-anchor" href="#interface-fnconfig" aria-label="Permalink to “FnConfig”">​</a></h3><p>Structured description of all <code>pika()</code> call variants derived from a base function name.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>fnName</code></td><td><code>string</code></td><td>The configured base function name (e.g. <code>&#39;pika&#39;</code>).</td><td>—</td></tr><tr><td><code>previewFnName</code></td><td><code>string</code></td><td>The preview function name derived from the base name (e.g. <code>&#39;pikap&#39;</code>).</td><td>—</td></tr><tr><td><code>roots</code></td><td><code>ReadonlySet&lt;string&gt;</code></td><td>Root identifiers that make a callee a candidate macro call.</td><td>—</td></tr><tr><td><code>variants</code></td><td><code>ReadonlyMap&lt;string, FnVariant&gt;</code></td><td>All variants keyed by canonical dot-form name.</td><td>—</td></tr></tbody></table><p><strong>Remarks:</strong></p><p>Replaces the legacy regex-based <code>FnUtils</code> classification: the AST macro collector matches call sites against <code>roots</code> and looks classification up in <code>variants</code> instead of testing name strings against a compiled regex.</p><br><br><h3 id="type-fnoutputkind" tabindex="-1">FnOutputKind <a class="header-anchor" href="#type-fnoutputkind" aria-label="Permalink to “FnOutputKind”">​</a></h3><p>Output-format classification of a <code>pika()</code> call variant.</p><ul><li><code>&#39;normal&#39;</code> — output format follows the integration&#39;s <code>transformedFormat</code> option.</li><li><code>&#39;forceString&#39;</code> — always emits a space-joined string literal (<code>pika.str</code>).</li><li><code>&#39;forceArray&#39;</code> — always emits an array of string literals (<code>pika.arr</code>).</li></ul><p><strong>Type:</strong> <code>&quot;normal&quot; | &quot;forceString&quot; | &quot;forceArray&quot;</code></p><br><br><h3 id="interface-fnvariant" tabindex="-1">FnVariant <a class="header-anchor" href="#interface-fnvariant" aria-label="Permalink to “FnVariant”">​</a></h3><p>One recognized <code>pika()</code> call variant, derived from the configured base function name.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>name</code></td><td><code>string</code></td><td>Canonical dot-form name, e.g. <code>&#39;pika&#39;</code>, <code>&#39;pika.str&#39;</code>, <code>&#39;pikap.arr&#39;</code>.</td><td>—</td></tr><tr><td><code>root</code></td><td><code>string</code></td><td>Root identifier of the call site: the base function name or its preview counterpart.</td><td>—</td></tr><tr><td><code>property</code></td><td><code>&#39;str&#39; | &#39;arr&#39; | null</code></td><td>Member property of the variant, or <code>null</code> for bare calls.</td><td>—</td></tr><tr><td><code>kind</code></td><td><code>FnOutputKind</code></td><td>Output-format classification of this variant.</td><td>—</td></tr><tr><td><code>preview</code></td><td><code>boolean</code></td><td>Whether this is a preview variant (<code>pikap</code>, <code>pikap.str</code>, <code>pikap.arr</code>).</td><td>—</td></tr></tbody></table><p><strong>Remarks:</strong></p><p>Variants are identified by their canonical dot-form name (e.g. <code>&#39;pika.str&#39;</code>). Bracket-notation call sites (<code>pika[&#39;str&#39;]</code>, <code>pika[\`str\`]</code>) are normalized to the dot form by the macro collector before variant lookup, so bracket forms are never enumerated here.</p><br><br><h3 id="interface-frameworkprocessor" tabindex="-1">FrameworkProcessor <a class="header-anchor" href="#interface-frameworkprocessor" aria-label="Permalink to “FrameworkProcessor”">​</a></h3><p>A framework-specific source analyzer.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>name</code></td><td><code>string</code></td><td>Diagnostic name of the processor (e.g. <code>&#39;js&#39;</code>, <code>&#39;vue&#39;</code>).</td><td>—</td></tr><tr><td><code>analyze</code></td><td><code>(code: string, id: string, options: ProcessorOptions) =&gt; Promise&lt;AnalyzedModule&gt; | AnalyzedModule</code></td><td>Analyzes a module and returns every macro call in it.</td><td>—</td></tr></tbody></table><p><strong>Remarks:</strong></p><p>Processors only ANALYZE — they never rewrite. The pipeline applies all replacements itself so module transforms stay atomic. A processor must throw <code>PikaTransformError</code> on any parse/scope/evaluation failure; partial results are never returned. This is the extensibility seam for future framework support (svelte, astro, ...): implement this interface and register the extensions in the processor registry.</p><br><br><h3 id="interface-integrationcontext" tabindex="-1">IntegrationContext <a class="header-anchor" href="#interface-integrationcontext" aria-label="Permalink to “IntegrationContext”">​</a></h3><p>The main build-tool integration context that bridges the PikaCSS engine with bundler plugins.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>cwd</code></td><td><code>string</code></td><td>The current working directory. Can be updated at runtime (e.g., when the project root changes).</td><td>—</td></tr><tr><td><code>configErrorBehavior</code></td><td><code>&#39;throw&#39; | &#39;retain-last-good&#39;</code></td><td>How the context reacts to a config file that fails to evaluate or an engine that fails to build. Set by the bundler adapter from the build mode.</td><td>—</td></tr><tr><td><code>currentPackageName</code></td><td><code>string</code></td><td>The npm package name of the integration consumer, used in generated file headers and module declarations.</td><td>—</td></tr><tr><td><code>fnName</code></td><td><code>string</code></td><td>The base function name recognized in source transforms (e.g., <code>&#39;pika&#39;</code>).</td><td>—</td></tr><tr><td><code>transformedFormat</code></td><td><code>&#39;string&#39; | &#39;array&#39;</code></td><td>The default output format for normal <code>pika()</code> calls: <code>&#39;string&#39;</code> or <code>&#39;array&#39;</code>.</td><td>—</td></tr><tr><td><code>cssCodegenFilepath</code></td><td><code>string</code></td><td>Absolute path to the generated CSS output file, computed from <code>cwd</code> and the configured relative path.</td><td>—</td></tr><tr><td><code>tsCodegenFilepath</code></td><td><code>string | Nullish</code></td><td>Absolute path to the generated TypeScript declaration file, or <code>null</code> if TypeScript codegen is disabled.</td><td>—</td></tr><tr><td><code>hasVue</code></td><td><code>boolean</code></td><td>Whether the <code>vue</code> package is installed in the project, used to include Vue-specific type declarations in codegen.</td><td>—</td></tr><tr><td><code>resolvedConfig</code></td><td><code>EngineConfig | Nullish</code></td><td>The loaded engine configuration object, or <code>null</code> if loading failed or no config was found.</td><td>—</td></tr><tr><td><code>resolvedConfigPath</code></td><td><code>string | Nullish</code></td><td>Absolute path to the resolved config file on disk, or <code>null</code> for inline configs or when no config was loaded.</td><td>—</td></tr><tr><td><code>resolvedConfigContent</code></td><td><code>string | Nullish</code></td><td>Raw string content of the config file, or <code>null</code> for inline configs or when no config was loaded.</td><td>—</td></tr><tr><td><code>loadConfig</code></td><td><code>() =&gt; Promise&lt;LoadedConfigResult&gt;</code></td><td>Loads (or reloads) the engine configuration from disk or inline source, updating <code>resolvedConfig</code>, <code>resolvedConfigPath</code>, and <code>resolvedConfigContent</code>.</td><td>—</td></tr><tr><td><code>usages</code></td><td><code>Map&lt;string, UsageRecord[]&gt;</code></td><td>Map from source file ID to the list of <code>UsageRecord</code> entries extracted during transforms. Keyed by the normalized absolute file path (<code>parseModuleId(...).file</code>).</td><td>—</td></tr><tr><td><code>previewUsages</code></td><td><code>Map&lt;string, UsageRecord[]&gt;</code></td><td>Map from source file ID to preview-only <code>UsageRecord</code> entries (from <code>pikap()</code> calls). Only these drive TypeScript preview overload generation.</td><td>—</td></tr><tr><td><code>hooks</code></td><td><code>{ styleUpdated: ReturnType&lt;typeof createEventHook&lt;void&gt;&gt; tsCodegenUpdated: ReturnType&lt;typeof createEventHook&lt;void&gt;&gt; }</code></td><td>Event hooks for notifying plugins when generated outputs need refreshing. <code>styleUpdated</code> fires on CSS changes; <code>tsCodegenUpdated</code> fires on TypeScript declaration changes.</td><td>—</td></tr><tr><td><code>engine</code></td><td><code>Engine</code></td><td>The initialized PikaCSS engine instance. Throws if accessed before <code>setup()</code> completes.</td><td>—</td></tr><tr><td><code>transformFilter</code></td><td><code>{ include: string[] exclude: string[] }</code></td><td>Glob patterns for the bundler&#39;s transform pipeline, derived from the scan config with codegen files excluded.</td><td>—</td></tr><tr><td><code>isTransformTarget</code></td><td><code>(id: string) =&gt; boolean</code></td><td>Returns whether a module id should be transformed, evaluated against the CURRENT <code>cwd</code>.</td><td>—</td></tr><tr><td><code>isIdle</code></td><td><code>boolean</code></td><td>Whether no <code>transform()</code> calls are currently in flight.</td><td>—</td></tr><tr><td><code>waitForIdle</code></td><td><code>() =&gt; Promise&lt;void&gt;</code></td><td>Resolves once all in-flight <code>transform()</code> calls have settled.</td><td>—</td></tr><tr><td><code>transform</code></td><td><code>(code: string, id: string) =&gt; Promise&lt;{ code: string, map: SourceMap } | Nullish&gt;</code></td><td>Processes a source file by extracting <code>pika()</code> calls via the AST compiler, resolving them through the engine, and replacing them with computed output.</td><td>—</td></tr><tr><td><code>dropModule</code></td><td><code>(id: string) =&gt; void</code></td><td>Drops all state for a module (usages, preview usages, prepared results), e.g. when the bundler reports the file as deleted. Accepts raw bundler ids (relative paths, query/hash suffixes) and normalizes them internally. Queues output regeneration when styles were dropped.</td><td>—</td></tr><tr><td><code>getScannedButNotTransformedFiles</code></td><td><code>() =&gt; string[]</code></td><td>Returns the physical files whose styles entered the generated CSS during the build-mode full scan but that the bundler&#39;s own transform pass never reached — dead files or files missing from the import graph. Sorted; empty in dev mode (no full scan).</td><td>—</td></tr><tr><td><code>getCssCodegenContent</code></td><td><code>() =&gt; Promise&lt;string | Nullish&gt;</code></td><td>Generates the full CSS output string, including layer declarations, preflights, and all atomic styles collected from transforms.</td><td>—</td></tr><tr><td><code>getTsCodegenContent</code></td><td><code>() =&gt; Promise&lt;string | Nullish&gt;</code></td><td>Generates the full TypeScript declaration content for <code>pika.gen.ts</code>, or <code>null</code> if TypeScript codegen is disabled.</td><td>—</td></tr><tr><td><code>writeCssCodegenFile</code></td><td><code>() =&gt; Promise&lt;void&gt;</code></td><td>Generates and writes the CSS codegen file to disk at <code>cssCodegenFilepath</code>.</td><td>—</td></tr><tr><td><code>writeTsCodegenFile</code></td><td><code>() =&gt; Promise&lt;void&gt;</code></td><td>Generates and writes the TypeScript codegen file to disk at <code>tsCodegenFilepath</code>. No-op if TypeScript codegen is disabled.</td><td>—</td></tr><tr><td><code>fullyCssCodegen</code></td><td><code>() =&gt; Promise&lt;void&gt;</code></td><td>Scans all matching source files, collects usages via transform, then writes the CSS codegen file. Used for full rebuilds.</td><td>—</td></tr><tr><td><code>setupPromise</code></td><td><code>Promise&lt;void&gt; | null</code></td><td>The pending setup promise while initialization is in progress, or <code>null</code> when idle. Transform calls await this before proceeding.</td><td>—</td></tr><tr><td><code>setup</code></td><td><code>() =&gt; Promise&lt;void&gt;</code></td><td>Initializes (or reinitializes) the context by clearing state, loading config, creating the engine, and wiring up dev hooks. Returns a promise that resolves when setup is complete.</td><td>—</td></tr></tbody></table><p><strong>Remarks:</strong></p><p>Created via <code>createCtx()</code>. The context manages the full build lifecycle: config loading, engine initialization, source file transformation, usage tracking, and output file generation. All transform and codegen calls automatically await <code>setup()</code> completion before proceeding.</p><br><br><h3 id="interface-integrationcontextoptions" tabindex="-1">IntegrationContextOptions <a class="header-anchor" href="#interface-integrationcontextoptions" aria-label="Permalink to “IntegrationContextOptions”">​</a></h3><p>Configuration options for creating an integration context.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>cwd</code></td><td><code>string</code></td><td>The working directory used to resolve relative paths for config files, codegen outputs, and source scanning.</td><td>—</td></tr><tr><td><code>currentPackageName</code></td><td><code>string</code></td><td>The npm package name of the integration consumer (e.g., <code>&#39;@pikacss/unplugin-pikacss&#39;</code>), embedded in generated file headers and import paths.</td><td>—</td></tr><tr><td><code>scan</code></td><td><code>{ include: string[] exclude: string[] }</code></td><td>Glob patterns controlling which source files are scanned for <code>pika()</code> calls. <code>include</code> specifies files to process; <code>exclude</code> specifies files to skip.</td><td>—</td></tr><tr><td><code>configOrPath</code></td><td><code>EngineConfig | string | Nullish</code></td><td>The engine configuration object, a path to a config file, or <code>null</code>/<code>undefined</code> to trigger auto-discovery of <code>pika.config.*</code> files.</td><td>—</td></tr><tr><td><code>fnName</code></td><td><code>string</code></td><td>The base function name to recognize in source code (e.g., <code>&#39;pika&#39;</code>). All variants (<code>.str</code>, <code>.arr</code>, preview) are derived from this name.</td><td>—</td></tr><tr><td><code>transformedFormat</code></td><td><code>&#39;string&#39; | &#39;array&#39;</code></td><td>Controls the default output format of normal <code>pika()</code> calls: <code>&#39;string&#39;</code> produces a space-joined class string, <code>&#39;array&#39;</code> produces a string array.</td><td>—</td></tr><tr><td><code>tsCodegen</code></td><td><code>false | string</code></td><td>Path to the generated TypeScript declaration file (<code>pika.gen.ts</code>), or <code>false</code> to disable TypeScript codegen entirely.</td><td>—</td></tr><tr><td><code>cssCodegen</code></td><td><code>string</code></td><td>Path to the generated CSS output file (e.g., <code>&#39;pika.gen.css&#39;</code>).</td><td>—</td></tr><tr><td><code>autoCreateConfig</code></td><td><code>boolean</code></td><td>When <code>true</code>, automatically scaffolds a default <code>pika.config.js</code> file if no config file is found.</td><td>—</td></tr><tr><td><code>onDiagnostic?</code></td><td><code>DiagnosticHandler</code></td><td>Receives engine diagnostics. Defaults to the official console adapter.</td><td>—</td></tr></tbody></table><p><strong>Remarks:</strong></p><p>These options are set by bundler plugin adapters (Vite, webpack, Nuxt) and are not typically configured by end users directly.</p><br><br><h3 id="type-jsdialect" tabindex="-1">JsDialect <a class="header-anchor" href="#type-jsdialect" aria-label="Permalink to “JsDialect”">​</a></h3><p>JavaScript dialect a source chunk is parsed as.</p><p><strong>Type:</strong> <code>&quot;js&quot; | &quot;jsx&quot; | &quot;ts&quot; | &quot;tsx&quot;</code></p><p><strong>Remarks:</strong></p><p><code>.ts</code> sources must NOT enable the <code>jsx</code> plugin: TypeScript angle-bracket casts (<code>&lt;T&gt;expr</code>) are only parseable without it. <code>.tsx</code> enables both.</p><br><br><h3 id="type-loadedconfigresult" tabindex="-1">LoadedConfigResult <a class="header-anchor" href="#type-loadedconfigresult" aria-label="Permalink to “LoadedConfigResult”">​</a></h3><p>Discriminated union representing the outcome of loading an engine configuration file.</p><p><strong>Remarks:</strong></p><p>Four shapes are possible: an inline config (no file), a successfully loaded file-based config, a file that exists but failed to evaluate (path and content kept so integrations can watch it and reload after a fix), or a missing load (all fields <code>null</code>). The <code>file</code> and <code>content</code> fields are populated whenever the config file was found on disk, enabling hot-reload detection.</p><br><br><h3 id="interface-macrocall" tabindex="-1">MacroCall <a class="header-anchor" href="#interface-macrocall" aria-label="Permalink to “MacroCall”">​</a></h3><p>A fully analyzed <code>pika()</code> macro call: its variant, source range, and statically evaluated arguments.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>variant</code></td><td><code>FnVariant</code></td><td>The matched call variant.</td><td>—</td></tr><tr><td><code>start</code></td><td><code>number</code></td><td>Zero-based character offset where the call begins in the module source.</td><td>—</td></tr><tr><td><code>end</code></td><td><code>number</code></td><td>Zero-based character offset one past the call&#39;s closing parenthesis (exclusive).</td><td>—</td></tr><tr><td><code>loc</code></td><td><code>{ line: number, column: number }</code></td><td>One-based position of the call, for diagnostics.</td><td>—</td></tr><tr><td><code>args</code></td><td><code>Parameters&lt;Engine[&#39;use&#39;]&gt;</code></td><td>Statically evaluated <code>engine.use()</code> arguments (plain data by construction).</td><td>—</td></tr><tr><td><code>quote</code></td><td><code>&#39;&quot;&#39; | &#39;\\&#39;&#39;</code></td><td>Quote character for the emitted literal at this site (<code>&#39;</code> for JS sources; AST-derived in Vue templates).</td><td>—</td></tr></tbody></table><br><br><h3 id="interface-parsedmoduleid" tabindex="-1">ParsedModuleId <a class="header-anchor" href="#interface-parsedmoduleid" aria-label="Permalink to “ParsedModuleId”">​</a></h3><p>Normalized identity of a bundler module id.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>file</code></td><td><code>string</code></td><td>Normalized absolute file path with query/hash stripped.</td><td>—</td></tr><tr><td><code>query</code></td><td><code>string | null</code></td><td>Raw query string without the leading <code>?</code>, or <code>null</code> when the id has none.</td><td>—</td></tr><tr><td><code>ext</code></td><td><code>string</code></td><td>Lowercase file extension without the leading dot, or <code>&#39;&#39;</code> when the file has none.</td><td>—</td></tr></tbody></table><p><strong>Remarks:</strong></p><p>Bundler ids come in many shapes for the same physical file: absolute or cwd-relative paths, ids with query strings (<code>App.vue?vue&amp;type=script</code>), and hash suffixes. All per-module state (usages, prepared results) must be keyed by the same canonical form, which is <code>file</code>.</p><br><br><h3 id="interface-parseoffsets" tabindex="-1">ParseOffsets <a class="header-anchor" href="#interface-parseoffsets" aria-label="Permalink to “ParseOffsets”">​</a></h3><p>Position offsets applied to all emitted node positions, used when parsing an embedded source chunk (e.g. a Vue SFC block) so node offsets/locations are absolute into the surrounding file.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>startIndex?</code></td><td><code>number</code></td><td>Zero-based character offset of the chunk inside the surrounding file.</td><td>—</td></tr><tr><td><code>startLine?</code></td><td><code>number</code></td><td>One-based line of the chunk&#39;s first character.</td><td>—</td></tr><tr><td><code>startColumn?</code></td><td><code>number</code></td><td>Zero-based column of the chunk&#39;s first character.</td><td>—</td></tr></tbody></table><br><br><h3 id="type-processorloader" tabindex="-1">ProcessorLoader <a class="header-anchor" href="#type-processorloader" aria-label="Permalink to “ProcessorLoader”">​</a></h3><p>Lazily loads a FrameworkProcessor; heavyweight parser dependencies are only imported when a matching file is actually analyzed.</p><br><br><h3 id="interface-processoroptions" tabindex="-1">ProcessorOptions <a class="header-anchor" href="#interface-processoroptions" aria-label="Permalink to “ProcessorOptions”">​</a></h3><p>Options handed to a processor&#39;s <code>analyze</code>.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>fnConfig</code></td><td><code>FnConfig</code></td><td>The variant config derived from the configured base function name.</td><td>—</td></tr></tbody></table><br><br><h3 id="interface-processorregistry" tabindex="-1">ProcessorRegistry <a class="header-anchor" href="#interface-processorregistry" aria-label="Permalink to “ProcessorRegistry”">​</a></h3><p>Registry mapping file extensions to framework processors.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>register</code></td><td><code>(extensions: string[], loader: ProcessorLoader) =&gt; void</code></td><td>Registers a lazy processor for the given extensions (leading dots optional, case-insensitive).</td><td>—</td></tr><tr><td><code>resolve</code></td><td><code>(ext: string) =&gt; Promise&lt;FrameworkProcessor&gt; | null</code></td><td>Resolves the processor for an extension, or <code>null</code> when none is registered. Loaded processors are memoized.</td><td>—</td></tr><tr><td><code>has</code></td><td><code>(ext: string) =&gt; boolean</code></td><td>Returns whether a processor is registered for the extension.</td><td>—</td></tr></tbody></table><br><br><h3 id="interface-transformerrorloc" tabindex="-1">TransformErrorLoc <a class="header-anchor" href="#interface-transformerrorloc" aria-label="Permalink to “TransformErrorLoc”">​</a></h3><p>One-based source position of a transform failure.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>line</code></td><td><code>number</code></td><td>One-based line number of the failure.</td><td>—</td></tr><tr><td><code>column</code></td><td><code>number</code></td><td>Zero-based column of the failure (Babel convention).</td><td>—</td></tr></tbody></table><br><br><h3 id="type-transformerrorstage" tabindex="-1">TransformErrorStage <a class="header-anchor" href="#type-transformerrorstage" aria-label="Permalink to “TransformErrorStage”">​</a></h3><p>Pipeline stage in which a transform failure occurred.</p><ul><li><code>&#39;parse&#39;</code> — source (or an embedded expression) failed to parse.</li><li><code>&#39;collect&#39;</code> — the macro-call collector rejected a call site.</li><li><code>&#39;evaluate&#39;</code> — a call argument is not statically evaluable.</li><li><code>&#39;prepare&#39;</code> — resolving a call through the engine failed.</li></ul><p><strong>Type:</strong> <code>&quot;parse&quot; | &quot;collect&quot; | &quot;evaluate&quot; | &quot;prepare&quot;</code></p><br><br><h3 id="interface-usagerecord" tabindex="-1">UsageRecord <a class="header-anchor" href="#interface-usagerecord" aria-label="Permalink to “UsageRecord”">​</a></h3><p>Records a single <code>pika()</code> call result, pairing the resolved atomic style IDs with the original call arguments.</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>atomicStyleIds</code></td><td><code>string[]</code></td><td>The list of atomic CSS class names generated by the engine for this call.</td><td>—</td></tr><tr><td><code>params</code></td><td><code>Parameters&lt;Engine[&#39;use&#39;]&gt;</code></td><td>The original arguments passed to <code>engine.use()</code>, preserved for TypeScript codegen overload generation.</td><td>—</td></tr></tbody></table><p><strong>Remarks:</strong></p><p>Each source file may produce multiple <code>UsageRecord</code> entries — one per <code>pika()</code> call site. These records drive both CSS output (via <code>atomicStyleIds</code>) and IDE preview overloads (via <code>params</code>).</p><br><br><h2 id="next" tabindex="-1">Next <a class="header-anchor" href="#next" aria-label="Permalink to “Next”">​</a></h2><ul><li><a href="/integrations/unplugin.html">Unplugin integration</a></li><li><a href="/api/unplugin.html">Unplugin API reference</a></li><li><a href="/api/">API reference overview</a></li></ul>`,232)])])}const u=t(r,[["render",i]]);export{g as __pageData,u as default};
