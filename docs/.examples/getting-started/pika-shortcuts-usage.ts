// Shortcuts are string-based style references defined in your config.
// Pass them directly to pika() alongside style objects.
const className = pika(
	'flex-center', // shortcut defined in config
	{ gap: '1rem' }, // additional inline styles
)
