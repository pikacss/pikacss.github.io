const cardClass = pika({
	backgroundColor: 'white',
	borderRadius: '0.75rem',
	boxShadow: '0 10px 30px rgba(15, 23, 42, 0.08)',
})

const badgeClass = pika({
	backgroundColor: 'white',
	borderRadius: '0.75rem',
	padding: '0.25rem 0.5rem',
})
