export const shortcutStringUsage = 'chip'

const centered = pika('flex-center')

const card = pika(
	'flex-center',
	{ gap: '1rem', padding: '2rem' },
)

const spaced = pika('m-4', { color: 'blue' })
