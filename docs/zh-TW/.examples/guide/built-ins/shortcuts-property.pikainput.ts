export const propertyShortcutUsage = {
	__shortcut: ['chip'],
	color: '#0f172a',
}

const centered = pika({
	__shortcut: 'flex-center',
	gap: '1rem',
})

const button = pika({
	__shortcut: ['flex-center', 'btn'],
	backgroundColor: '#0ea5e9',
	color: 'white',
})

const spacing = pika({
	__shortcut: 'm-4',
})
