const defaultOutput = pika({ display: 'inline-flex', gap: '0.5rem' })

const forcedString = pika.str({
	backgroundColor: '#0ea5e9',
	color: 'white',
})

const forcedArray = pika.arr({
	padding: '0.5rem 0.75rem',
	borderRadius: '9999px',
})
